# Homepage Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Натянуть дизайн главной (`/uk`, `/en`) на существующий Page Builder, превратив 4 голых блока-стуба в полноценные 8 типов блоков с 10-секционной композицией согласно `docs/superpowers/design-sources/levant-parfums/project/pages-home.jsx`.

**Architecture:** Расширение существующего Page Builder (`docs/superpowers/specs/2026-05-23-page-builder-design.md`) — никаких миграций БД, никаких новых таблиц. Всё новое контентом живёт внутри JSON `pages.blocks`. Добавляем 4 новых типа Block-классов (`brand_story`, `series_duo`, `pillars`, `testimonials`), расширяем 4 существующих (`hero`, `text`, `products`, `articles`). Visual ритм — фоновыми токенами (`bg-2` для manifesto / Guide-pillars / testimonials).

**Tech Stack:** Laravel 13, Filament 5 (Builder + Repeater + Tabs), Spatie translatable (для модели), кастомный `TranslatableTabs` helper (для translatable полей внутри JSON), Tailwind v4 + кастомный site CSS, Alpine.js (без новых JS), Pest 4 + RefreshDatabase.

**Spec:** `docs/superpowers/specs/2026-05-24-homepage-design.md`. Перед началом каждой задачи прочитать соответствующий раздел спеки.

**Copy source:** `docs/superpowers/design-sources/levant-parfums/project/data.js` (ключи `t.hero_*`, `t.manifesto_*`, `t.threepoints_*`, `t.collections_*`, `t.guide_*`, `t.bestsellers_*`, `t.advantages_*`, `t.adv1_*`/`adv2_*`/`adv3_*`, `t.reviews_*`, `t.blog_*`, `t.collection_view`).

**CSS source:** `docs/superpowers/design-sources/levant-parfums/project/styles.css` (строки 329-602 для всех целевых блоков; адаптируем токены `--bg`, `--accent` без изменений — они идентичны нашему `theme-cream`).

---

## File Structure

**Создаются:**
- `app/Filament/Resources/Pages/Schemas/Blocks/BrandStoryBlock.php`
- `app/Filament/Resources/Pages/Schemas/Blocks/SeriesDuoBlock.php`
- `app/Filament/Resources/Pages/Schemas/Blocks/PillarsBlock.php`
- `app/Filament/Resources/Pages/Schemas/Blocks/TestimonialsBlock.php`
- `resources/views/pages/blocks/brand_story.blade.php`
- `resources/views/pages/blocks/series_duo.blade.php`
- `resources/views/pages/blocks/pillars.blade.php`
- `resources/views/pages/blocks/testimonials.blade.php`
- `resources/views/components/site/article-card.blade.php`
- `resources/views/components/site/review-card.blade.php`
- `resources/views/components/site/pillar.blade.php`
- `resources/css/site/components/hero.css`
- `resources/css/site/components/manifesto.css`
- `resources/css/site/components/three-points.css`
- `resources/css/site/components/collections.css`
- `resources/css/site/components/pillars.css`
- `resources/css/site/components/testimonials.css`
- `resources/css/site/components/article-card.css`
- `database/seeders/images/levant-luxury-bottle.jpg` (скопировать из design-sources)
- `database/seeders/images/levant-flacon-3.jpg` (скопировать из design-sources)
- `database/seeders/images/levant-flacon-4.jpg` (скопировать из design-sources)
- `tests/Feature/Content/HomePageRenderTest.php`
- `tests/Feature/Content/Filament/PageBuilderHomeTest.php`

**Модифицируются:**
- `app/Enums/BlockType.php` — +4 case
- `app/Filament/Resources/Pages/Schemas/Blocks/HeroBlock.php` — +eyebrow/title_top/title_bottom/lead/floating/meta/second cta
- `app/Filament/Resources/Pages/Schemas/Blocks/TextBlock.php` — +eyebrow/signature, body → Textarea (не markdown)
- `app/Filament/Resources/Pages/Schemas/Blocks/ProductsBlock.php` — +eyebrow/title/cta
- `app/Filament/Resources/Pages/Schemas/Blocks/ArticlesBlock.php` — +eyebrow/title/cta, repeater minItems=3 maxItems=3
- `app/Filament/Resources/Pages/Schemas/PageForm.php` — добавить 4 новых блока в `Builder::blocks()`
- `resources/views/pages/blocks/hero.blade.php` — переписать
- `resources/views/pages/blocks/text.blade.php` — переписать (manifesto)
- `resources/views/pages/blocks/products.blade.php` — переписать (slider + header)
- `resources/views/pages/blocks/articles.blade.php` — переписать (grid 3)
- `resources/css/site/index.css` — +7 @import
- `lang/uk/content.php` + `lang/en/content.php` — +4 block labels, ~20 fields, surface labels
- `database/seeders/Content/PageSeeder.php` — полностью переписать массив blocks
- `database/seeders/DatabaseSeeder.php` — реордер `ArticleSeeder` перед `PageSeeder`

---

## Execution Conventions

- **TDD:** Если задача упоминает тест — пишем тест **до** реализации, прогоняем и видим RED, потом имплементим.
- **Pest:** Test runner: `php artisan test --filter='it renders'` (по имени), `composer test` (полный). Никаких `RefreshDatabase` в `it()` — auto-applied в `tests/Pest.php`.
- **Commit per task:** Один task = один смысловой коммит. Если task большой (новый Block + новый партиал + CSS), разрешается 2-3 коммита внутри одного task'а, явно отмечено в шагах.
- **Helper `$t`:** Во всех новых партиалах используется helper `$t = function (string $key) use ($data, $locale) { $value = $data[$key][$locale] ?? null; return filled($value) ? $value : ($data[$key]['uk'] ?? ''); };`. Это **обязательно** — `??` не справляется с пустой строкой, см. § revisions в спеке.
- **Сервер:** `composer dev` запускает artisan serve + queue + pail + vite параллельно (для manual verify). Запускаем один раз в начале сессии, не в каждой задаче.

---

## Task 1: Расширить `BlockType` enum

**Files:**
- Modify: `app/Enums/BlockType.php`

Добавляем 4 новых case'а в enum. Метод `label()` уже завязан на `trans("content.blocks.{$this->value}.label")` — переводы добавим в Task 2.

- [ ] **Step 1: Открыть `app/Enums/BlockType.php` и заменить блок case'ов**

```php
<?php

namespace App\Enums;

enum BlockType: string
{
    case Hero = 'hero';
    case Text = 'text';
    case Products = 'products';
    case BrandStory = 'brand_story';
    case SeriesDuo = 'series_duo';
    case Pillars = 'pillars';
    case Testimonials = 'testimonials';
    case Articles = 'articles';

    public function label(): string
    {
        return trans("content.blocks.{$this->value}.label");
    }
}
```

Порядок case'ов соответствует порядку в Builder (это же порядок появления в UI «Add block»).

- [ ] **Step 2: Прогнать существующие тесты, чтобы убедиться что ничего не сломалось**

Run: `php artisan test --filter='Page' -v`
Expected: PASS (старые тесты на `Page` не используют новые case'ы).

- [ ] **Step 3: Коммит**

```bash
git add app/Enums/BlockType.php
git commit -m "feat(homepage): extend BlockType enum with 4 new types"
```

---

## Task 2: Переводы — расширить `lang/{uk,en}/content.php`

**Files:**
- Modify: `lang/uk/content.php`
- Modify: `lang/en/content.php`

Добавляем 4 новых block label, ~20 новых field-меток, surface labels, и `add_item` для новых repeater'ов. Это нужно **до** Filament Block-классов, иначе админка покажет пустые заголовки.

- [ ] **Step 1: Открыть `lang/uk/content.php` и обновить секцию `'blocks'`**

Заменить весь массив `'blocks' => [...]` на:

```php
'blocks' => [
    'hero' => [
        'label' => 'Hero-блок',
    ],
    'text' => [
        'label' => 'Маніфест / редакторський текст',
    ],
    'products' => [
        'label' => 'Список товарів',
        'add_item' => 'Додати товар',
    ],
    'brand_story' => [
        'label' => 'Бренд-сторі (три точки)',
        'add_pillar' => 'Додати точку',
    ],
    'series_duo' => [
        'label' => 'Дві колекції (Onyx × Luxury)',
        'add_item' => 'Додати колекцію',
    ],
    'pillars' => [
        'label' => 'Гайд / Переваги (нумерована сітка)',
        'add_item' => 'Додати пункт',
    ],
    'testimonials' => [
        'label' => 'Відгуки (слайдер)',
        'add_item' => 'Додати відгук',
    ],
    'articles' => [
        'label' => 'Список статей',
        'add_item' => 'Додати статтю',
    ],
    'fields' => [
        // existing common
        'is_visible' => 'Показувати блок',
        'anchor' => 'Якір (id у URL)',

        // existing block-level (used widely)
        'title' => 'Заголовок',
        'subtitle' => 'Підзаголовок',
        'body' => 'Текст',
        'cta_label' => 'Текст кнопки',
        'cta_url' => 'Посилання кнопки',
        'image_path' => 'Зображення',
        'product_id' => 'Товар',
        'article_id' => 'Стаття',
        'cta_url_helper' => 'Можна вписати внутрішнє "/products" або зовнішнє "https://...".',

        // NEW common across blocks
        'eyebrow' => 'Eyebrow (надзаголовок)',
        'lead' => 'Ліб (короткий абзац)',
        'signature' => 'Підпис',
        'kicker' => 'Кікер',
        'description' => 'Опис',
        'surface' => 'Фон секції',

        // hero
        'title_top' => 'Заголовок (верхній рядок)',
        'title_bottom' => 'Заголовок (нижній рядок, курсив)',
        'floating_label' => 'Парящий ярлик',
        'meta' => 'Метрики (3 числа)',
        'meta_num' => 'Число',
        'meta_label' => 'Підпис',
        'secondary_cta_label' => 'Текст другої кнопки',
        'secondary_cta_url' => 'Посилання другої кнопки',

        // brand_story
        'pillars' => 'Точки',
        'pillar_label' => 'Назва',
        'pillar_caption' => 'Підпис',

        // series_duo
        'series_id' => 'Серія',

        // testimonials
        'quote' => 'Цитата',
        'author' => 'Автор',
        'city' => 'Місто',
        'rating' => 'Оцінка (1-5)',
    ],
    'surface' => [
        'default' => 'Без фону (default)',
        'tinted' => 'Затемнений фон (tinted)',
    ],
],
```

- [ ] **Step 2: Тот же набор ключей в `lang/en/content.php`**

Заменить `'blocks' => [...]` на тот же массив, но с английскими переводами. Дословный перевод — на ваше усмотрение, ниже опорный набор:

```php
'blocks' => [
    'hero' => ['label' => 'Hero block'],
    'text' => ['label' => 'Manifesto / editorial text'],
    'products' => ['label' => 'Product list', 'add_item' => 'Add product'],
    'brand_story' => ['label' => 'Brand story (three points)', 'add_pillar' => 'Add point'],
    'series_duo' => ['label' => 'Two collections (Onyx × Luxury)', 'add_item' => 'Add collection'],
    'pillars' => ['label' => 'Guide / Advantages (numbered grid)', 'add_item' => 'Add item'],
    'testimonials' => ['label' => 'Testimonials (slider)', 'add_item' => 'Add testimonial'],
    'articles' => ['label' => 'Article list', 'add_item' => 'Add article'],
    'fields' => [
        'is_visible' => 'Show block',
        'anchor' => 'Anchor (URL id)',
        'title' => 'Title',
        'subtitle' => 'Subtitle',
        'body' => 'Body',
        'cta_label' => 'CTA label',
        'cta_url' => 'CTA URL',
        'image_path' => 'Image',
        'product_id' => 'Product',
        'article_id' => 'Article',
        'cta_url_helper' => 'Internal "/products" or absolute "https://..." both work.',

        'eyebrow' => 'Eyebrow',
        'lead' => 'Lead paragraph',
        'signature' => 'Signature',
        'kicker' => 'Kicker',
        'description' => 'Description',
        'surface' => 'Section surface',

        'title_top' => 'Title (top line)',
        'title_bottom' => 'Title (bottom line, italic)',
        'floating_label' => 'Floating tag',
        'meta' => 'Meta numbers (3 items)',
        'meta_num' => 'Number',
        'meta_label' => 'Label',
        'secondary_cta_label' => 'Secondary CTA label',
        'secondary_cta_url' => 'Secondary CTA URL',

        'pillars' => 'Points',
        'pillar_label' => 'Label',
        'pillar_caption' => 'Caption',

        'series_id' => 'Series',

        'quote' => 'Quote',
        'author' => 'Author',
        'city' => 'City',
        'rating' => 'Rating (1-5)',
    ],
    'surface' => [
        'default' => 'Plain (default)',
        'tinted' => 'Tinted background',
    ],
],
```

- [ ] **Step 3: Очистить view-кэш и убедиться что переводы подгружаются**

Run: `php artisan view:clear && php artisan config:clear && php artisan test --filter='Page' -v`
Expected: PASS (тесты не упали — значит переводы валидно подгрузились).

- [ ] **Step 4: Коммит**

```bash
git add lang/uk/content.php lang/en/content.php
git commit -m "feat(homepage): translations for new block types and fields"
```

---

## Task 3: `BrandStoryBlock` — Filament Block-класс

**Files:**
- Create: `app/Filament/Resources/Pages/Schemas/Blocks/BrandStoryBlock.php`

Новый блок: eyebrow, title, body, и repeater `pillars` (ровно 3 точки) с полями `label`, `caption`. Все translatable через `TranslatableTabs`. Repeater min/max через `minItems(3)->maxItems(3)`, `defaultItems(3)` чтобы стартовый блок уже имел 3 пустых слота.

- [ ] **Step 1: Создать файл**

```php
<?php

namespace App\Filament\Resources\Pages\Schemas\Blocks;

use App\Filament\Resources\Pages\Schemas\Blocks\Concerns\TranslatableTabs;
use Filament\Forms\Components\Builder\Block;
use Filament\Forms\Components\Repeater;
use Filament\Forms\Components\Textarea;
use Filament\Forms\Components\TextInput;
use Filament\Forms\Components\Toggle;

class BrandStoryBlock
{
    public static function make(): Block
    {
        return Block::make('brand_story')
            ->label(trans('content.blocks.brand_story.label'))
            ->icon('heroicon-o-map')
            ->schema([
                ...self::commonFields(),
                TranslatableTabs::make('eyebrow'),
                TranslatableTabs::make('title', required: true),
                TranslatableTabs::make('body', component: Textarea::class),
                Repeater::make('pillars')
                    ->label(trans('content.blocks.fields.pillars'))
                    ->schema([
                        TranslatableTabs::make('pillar_label', required: true),
                        TranslatableTabs::make('pillar_caption'),
                    ])
                    ->minItems(3)
                    ->maxItems(3)
                    ->defaultItems(3)
                    ->addActionLabel(trans('content.blocks.brand_story.add_pillar'))
                    ->reorderable(false),
            ]);
    }

    protected static function commonFields(): array
    {
        return [
            Toggle::make('is_visible')
                ->label(trans('content.blocks.fields.is_visible'))
                ->default(true),
            TextInput::make('anchor')
                ->label(trans('content.blocks.fields.anchor'))
                ->prefix('#')
                ->alphaDash(),
        ];
    }
}
```

**Note:** Поля внутри pillars `pillar_label`/`pillar_caption` — это ключи внутри JSON `data.pillars[i]`. Это слегка путает (читатель ожидает просто `label`/`caption`), но `TranslatableTabs` использует имя поля одновременно как ключ перевода (`content.blocks.fields.{field}`) и как имя дотнотации (`{field}.uk`/`{field}.en`). Использовать общие имена `label`/`caption` нельзя — они слишком общие и могут конфликтовать с другими блоками. См. план перевода ключей в Task 2.

- [ ] **Step 2: Зарегистрировать блок в `PageForm`** — пока **не** делаем, делаем разом в Task 11.

- [ ] **Step 3: Коммит**

```bash
git add app/Filament/Resources/Pages/Schemas/Blocks/BrandStoryBlock.php
git commit -m "feat(homepage): BrandStoryBlock (three-points section)"
```

---

## Task 4: `SeriesDuoBlock` — Filament Block-класс

**Files:**
- Create: `app/Filament/Resources/Pages/Schemas/Blocks/SeriesDuoBlock.php`

Repeater из ровно 2 слотов (left/right). В каждом: Select по `Series`, FileUpload, kicker/title/description translatable, cta_label translatable.

- [ ] **Step 1: Создать файл**

```php
<?php

namespace App\Filament\Resources\Pages\Schemas\Blocks;

use App\Filament\Resources\Pages\Schemas\Blocks\Concerns\TranslatableTabs;
use App\Models\Catalogue\Series;
use Filament\Forms\Components\Builder\Block;
use Filament\Forms\Components\FileUpload;
use Filament\Forms\Components\Repeater;
use Filament\Forms\Components\Select;
use Filament\Forms\Components\Textarea;
use Filament\Forms\Components\TextInput;
use Filament\Forms\Components\Toggle;

class SeriesDuoBlock
{
    public static function make(): Block
    {
        return Block::make('series_duo')
            ->label(trans('content.blocks.series_duo.label'))
            ->icon('heroicon-o-squares-2x2')
            ->schema([
                ...self::commonFields(),
                TranslatableTabs::make('eyebrow'),
                TranslatableTabs::make('title', required: true),
                Repeater::make('items')
                    ->schema([
                        Select::make('series_id')
                            ->label(trans('content.blocks.fields.series_id'))
                            ->options(fn () => Series::query()
                                ->orderBy('sort_order')
                                ->get()
                                ->mapWithKeys(fn (Series $s) => [$s->id => $s->name])
                                ->all())
                            ->searchable()
                            ->required(),
                        FileUpload::make('image_path')
                            ->label(trans('content.blocks.fields.image_path'))
                            ->disk('public')
                            ->directory('pages/blocks')
                            ->image()
                            ->imageEditor()
                            ->maxSize(4096),
                        TranslatableTabs::make('kicker'),
                        TranslatableTabs::make('title'),
                        TranslatableTabs::make('description', component: Textarea::class),
                        TranslatableTabs::make('cta_label'),
                    ])
                    ->minItems(2)
                    ->maxItems(2)
                    ->defaultItems(2)
                    ->addActionLabel(trans('content.blocks.series_duo.add_item'))
                    ->reorderable(false),
            ]);
    }

    protected static function commonFields(): array
    {
        return [
            Toggle::make('is_visible')
                ->label(trans('content.blocks.fields.is_visible'))
                ->default(true),
            TextInput::make('anchor')
                ->label(trans('content.blocks.fields.anchor'))
                ->prefix('#')
                ->alphaDash(),
        ];
    }
}
```

- [ ] **Step 2: Коммит**

```bash
git add app/Filament/Resources/Pages/Schemas/Blocks/SeriesDuoBlock.php
git commit -m "feat(homepage): SeriesDuoBlock (collections section)"
```

---

## Task 5: `PillarsBlock` — Filament Block-класс (Guide + Advantages)

**Files:**
- Create: `app/Filament/Resources/Pages/Schemas/Blocks/PillarsBlock.php`

Universal numbered editorial block: `eyebrow`, `title`, `body` (optional lead), `surface` (default|tinted), `items` repeater (min 3, max 4) с полями `eyebrow`, `title`, `body`.

- [ ] **Step 1: Создать файл**

```php
<?php

namespace App\Filament\Resources\Pages\Schemas\Blocks;

use App\Filament\Resources\Pages\Schemas\Blocks\Concerns\TranslatableTabs;
use Filament\Forms\Components\Builder\Block;
use Filament\Forms\Components\Repeater;
use Filament\Forms\Components\Select;
use Filament\Forms\Components\Textarea;
use Filament\Forms\Components\TextInput;
use Filament\Forms\Components\Toggle;

class PillarsBlock
{
    public static function make(): Block
    {
        return Block::make('pillars')
            ->label(trans('content.blocks.pillars.label'))
            ->icon('heroicon-o-list-bullet')
            ->schema([
                ...self::commonFields(),
                Select::make('surface')
                    ->label(trans('content.blocks.fields.surface'))
                    ->options([
                        'default' => trans('content.blocks.surface.default'),
                        'tinted' => trans('content.blocks.surface.tinted'),
                    ])
                    ->default('default')
                    ->required(),
                TranslatableTabs::make('eyebrow'),
                TranslatableTabs::make('title', required: true),
                TranslatableTabs::make('body', component: Textarea::class),
                Repeater::make('items')
                    ->schema([
                        TranslatableTabs::make('eyebrow'),
                        TranslatableTabs::make('title', required: true),
                        TranslatableTabs::make('body', component: Textarea::class),
                    ])
                    ->minItems(3)
                    ->maxItems(4)
                    ->defaultItems(3)
                    ->addActionLabel(trans('content.blocks.pillars.add_item'))
                    ->reorderable(),
            ]);
    }

    protected static function commonFields(): array
    {
        return [
            Toggle::make('is_visible')
                ->label(trans('content.blocks.fields.is_visible'))
                ->default(true),
            TextInput::make('anchor')
                ->label(trans('content.blocks.fields.anchor'))
                ->prefix('#')
                ->alphaDash(),
        ];
    }
}
```

- [ ] **Step 2: Коммит**

```bash
git add app/Filament/Resources/Pages/Schemas/Blocks/PillarsBlock.php
git commit -m "feat(homepage): PillarsBlock (Guide and Advantages)"
```

---

## Task 6: `TestimonialsBlock` — Filament Block-класс

**Files:**
- Create: `app/Filament/Resources/Pages/Schemas/Blocks/TestimonialsBlock.php`

Eyebrow, title, опциональный CTA, repeater отзывов (min 2). Поле `author` — простой `TextInput` (имя не translatable), `city` — translatable, `quote` — translatable Textarea, `rating` — `TextInput::numeric()` с min/max.

- [ ] **Step 1: Создать файл**

```php
<?php

namespace App\Filament\Resources\Pages\Schemas\Blocks;

use App\Filament\Resources\Pages\Schemas\Blocks\Concerns\TranslatableTabs;
use Filament\Forms\Components\Builder\Block;
use Filament\Forms\Components\Repeater;
use Filament\Forms\Components\Textarea;
use Filament\Forms\Components\TextInput;
use Filament\Forms\Components\Toggle;

class TestimonialsBlock
{
    public static function make(): Block
    {
        return Block::make('testimonials')
            ->label(trans('content.blocks.testimonials.label'))
            ->icon('heroicon-o-chat-bubble-left-right')
            ->schema([
                ...self::commonFields(),
                TranslatableTabs::make('eyebrow'),
                TranslatableTabs::make('title', required: true),
                TranslatableTabs::make('cta_label'),
                TextInput::make('cta_url')
                    ->label(trans('content.blocks.fields.cta_url'))
                    ->maxLength(2048)
                    ->helperText(trans('content.blocks.fields.cta_url_helper')),
                Repeater::make('items')
                    ->schema([
                        TranslatableTabs::make('quote', required: true, component: Textarea::class),
                        TextInput::make('author')
                            ->label(trans('content.blocks.fields.author'))
                            ->required()
                            ->maxLength(120),
                        TranslatableTabs::make('city'),
                        TextInput::make('rating')
                            ->label(trans('content.blocks.fields.rating'))
                            ->numeric()
                            ->minValue(1)
                            ->maxValue(5),
                    ])
                    ->minItems(2)
                    ->defaultItems(2)
                    ->addActionLabel(trans('content.blocks.testimonials.add_item'))
                    ->reorderable(),
            ]);
    }

    protected static function commonFields(): array
    {
        return [
            Toggle::make('is_visible')
                ->label(trans('content.blocks.fields.is_visible'))
                ->default(true),
            TextInput::make('anchor')
                ->label(trans('content.blocks.fields.anchor'))
                ->prefix('#')
                ->alphaDash(),
        ];
    }
}
```

- [ ] **Step 2: Коммит**

```bash
git add app/Filament/Resources/Pages/Schemas/Blocks/TestimonialsBlock.php
git commit -m "feat(homepage): TestimonialsBlock (reviews slider)"
```

---

## Task 7: Расширить `HeroBlock`

**Files:**
- Modify: `app/Filament/Resources/Pages/Schemas/Blocks/HeroBlock.php`

Старые поля: `is_visible`, `anchor`, `title`, `subtitle`, `cta_label`, `cta_url`, `image_path`.

Новые поля: `eyebrow`, `title_top`, `title_bottom` (заменяют `title`), `lead` (заменяет `subtitle`), `floating_label`, `secondary_cta_label`, `secondary_cta_url`, `meta` repeater (ровно 3). Старые `title`/`subtitle` оставляем только в смысле «миграция данных через сидер» — в форме их не показываем (см. Task 18).

- [ ] **Step 1: Заменить содержимое файла**

```php
<?php

namespace App\Filament\Resources\Pages\Schemas\Blocks;

use App\Filament\Resources\Pages\Schemas\Blocks\Concerns\TranslatableTabs;
use Filament\Forms\Components\Builder\Block;
use Filament\Forms\Components\FileUpload;
use Filament\Forms\Components\Repeater;
use Filament\Forms\Components\Textarea;
use Filament\Forms\Components\TextInput;
use Filament\Forms\Components\Toggle;

class HeroBlock
{
    public static function make(): Block
    {
        return Block::make('hero')
            ->label(trans('content.blocks.hero.label'))
            ->icon('heroicon-o-rectangle-stack')
            ->schema([
                ...self::commonFields(),
                TranslatableTabs::make('eyebrow'),
                TranslatableTabs::make('title_top', required: true),
                TranslatableTabs::make('title_bottom'),
                TranslatableTabs::make('lead', component: Textarea::class),
                TranslatableTabs::make('floating_label'),

                TranslatableTabs::make('cta_label'),
                TextInput::make('cta_url')
                    ->label(trans('content.blocks.fields.cta_url'))
                    ->maxLength(2048)
                    ->helperText(trans('content.blocks.fields.cta_url_helper')),
                TranslatableTabs::make('secondary_cta_label'),
                TextInput::make('secondary_cta_url')
                    ->label(trans('content.blocks.fields.secondary_cta_url'))
                    ->maxLength(2048),

                FileUpload::make('image_path')
                    ->label(trans('content.blocks.fields.image_path'))
                    ->disk('public')
                    ->directory('pages/blocks')
                    ->image()
                    ->imageEditor()
                    ->maxSize(4096),

                Repeater::make('meta')
                    ->label(trans('content.blocks.fields.meta'))
                    ->schema([
                        TextInput::make('num')
                            ->label(trans('content.blocks.fields.meta_num'))
                            ->required()
                            ->maxLength(8),
                        TranslatableTabs::make('meta_label', required: true),
                    ])
                    ->minItems(3)
                    ->maxItems(3)
                    ->defaultItems(3)
                    ->reorderable(),
            ]);
    }

    protected static function commonFields(): array
    {
        return [
            Toggle::make('is_visible')
                ->label(trans('content.blocks.fields.is_visible'))
                ->default(true),
            TextInput::make('anchor')
                ->label(trans('content.blocks.fields.anchor'))
                ->prefix('#')
                ->alphaDash(),
        ];
    }
}
```

- [ ] **Step 2: Коммит**

```bash
git add app/Filament/Resources/Pages/Schemas/Blocks/HeroBlock.php
git commit -m "feat(homepage): extend HeroBlock with editorial split fields"
```

---

## Task 8: Перепрофилировать `TextBlock` под manifesto

**Files:**
- Modify: `app/Filament/Resources/Pages/Schemas/Blocks/TextBlock.php`

Старые поля: `title` (опц translatable), `body` (markdown translatable required).

Новые поля: `eyebrow`, `title` (italic required), `body` (Textarea — не markdown, разбиваем по `\n\n` в Blade), `signature` (опц italic gold).

- [ ] **Step 1: Заменить содержимое файла**

```php
<?php

namespace App\Filament\Resources\Pages\Schemas\Blocks;

use App\Filament\Resources\Pages\Schemas\Blocks\Concerns\TranslatableTabs;
use Filament\Forms\Components\Builder\Block;
use Filament\Forms\Components\Textarea;
use Filament\Forms\Components\TextInput;
use Filament\Forms\Components\Toggle;

class TextBlock
{
    public static function make(): Block
    {
        return Block::make('text')
            ->label(trans('content.blocks.text.label'))
            ->icon('heroicon-o-document-text')
            ->schema([
                ...self::commonFields(),
                TranslatableTabs::make('eyebrow'),
                TranslatableTabs::make('title', required: true),
                TranslatableTabs::make('body', required: true, component: Textarea::class),
                TranslatableTabs::make('signature'),
            ]);
    }

    protected static function commonFields(): array
    {
        return [
            Toggle::make('is_visible')
                ->label(trans('content.blocks.fields.is_visible'))
                ->default(true),
            TextInput::make('anchor')
                ->label(trans('content.blocks.fields.anchor'))
                ->prefix('#')
                ->alphaDash(),
        ];
    }
}
```

- [ ] **Step 2: Коммит**

```bash
git add app/Filament/Resources/Pages/Schemas/Blocks/TextBlock.php
git commit -m "feat(homepage): repurpose TextBlock as manifesto (eyebrow/signature, no markdown)"
```

---

## Task 9: Расширить `ProductsBlock` — header fields

**Files:**
- Modify: `app/Filament/Resources/Pages/Schemas/Blocks/ProductsBlock.php`

Добавить eyebrow / title / cta_label / cta_url. Repeater `items` оставить как есть.

- [ ] **Step 1: Заменить содержимое файла**

```php
<?php

namespace App\Filament\Resources\Pages\Schemas\Blocks;

use App\Filament\Resources\Pages\Schemas\Blocks\Concerns\TranslatableTabs;
use App\Models\Catalogue\Product;
use Filament\Forms\Components\Builder\Block;
use Filament\Forms\Components\Repeater;
use Filament\Forms\Components\Select;
use Filament\Forms\Components\TextInput;
use Filament\Forms\Components\Toggle;

class ProductsBlock
{
    public static function make(): Block
    {
        return Block::make('products')
            ->label(trans('content.blocks.products.label'))
            ->icon('heroicon-o-shopping-bag')
            ->schema([
                ...self::commonFields(),
                TranslatableTabs::make('eyebrow'),
                TranslatableTabs::make('title', required: true),
                TranslatableTabs::make('cta_label'),
                TextInput::make('cta_url')
                    ->label(trans('content.blocks.fields.cta_url'))
                    ->maxLength(2048)
                    ->helperText(trans('content.blocks.fields.cta_url_helper')),
                Repeater::make('items')
                    ->schema([
                        Select::make('product_id')
                            ->label(trans('content.blocks.fields.product_id'))
                            ->options(fn () => Product::query()
                                ->orderBy('slug')
                                ->get()
                                ->mapWithKeys(fn (Product $p) => [$p->id => $p->name])
                                ->all())
                            ->searchable()
                            ->required(),
                    ])
                    ->reorderable()
                    ->defaultItems(0)
                    ->addActionLabel(trans('content.blocks.products.add_item')),
            ]);
    }

    protected static function commonFields(): array
    {
        return [
            Toggle::make('is_visible')
                ->label(trans('content.blocks.fields.is_visible'))
                ->default(true),
            TextInput::make('anchor')
                ->label(trans('content.blocks.fields.anchor'))
                ->prefix('#')
                ->alphaDash(),
        ];
    }
}
```

- [ ] **Step 2: Коммит**

```bash
git add app/Filament/Resources/Pages/Schemas/Blocks/ProductsBlock.php
git commit -m "feat(homepage): ProductsBlock header fields (eyebrow/title/cta)"
```

---

## Task 10: Расширить `ArticlesBlock` — header fields + fixed 3 items

**Files:**
- Modify: `app/Filament/Resources/Pages/Schemas/Blocks/ArticlesBlock.php`

Добавить header (eyebrow/title/cta) + сделать repeater строго 3 items (`minItems(3)->maxItems(3)`).

- [ ] **Step 1: Заменить содержимое файла**

```php
<?php

namespace App\Filament\Resources\Pages\Schemas\Blocks;

use App\Filament\Resources\Pages\Schemas\Blocks\Concerns\TranslatableTabs;
use App\Models\Content\Article;
use Filament\Forms\Components\Builder\Block;
use Filament\Forms\Components\Repeater;
use Filament\Forms\Components\Select;
use Filament\Forms\Components\TextInput;
use Filament\Forms\Components\Toggle;

class ArticlesBlock
{
    public static function make(): Block
    {
        return Block::make('articles')
            ->label(trans('content.blocks.articles.label'))
            ->icon('heroicon-o-newspaper')
            ->schema([
                ...self::commonFields(),
                TranslatableTabs::make('eyebrow'),
                TranslatableTabs::make('title', required: true),
                TranslatableTabs::make('cta_label'),
                TextInput::make('cta_url')
                    ->label(trans('content.blocks.fields.cta_url'))
                    ->maxLength(2048)
                    ->helperText(trans('content.blocks.fields.cta_url_helper')),
                Repeater::make('items')
                    ->schema([
                        Select::make('article_id')
                            ->label(trans('content.blocks.fields.article_id'))
                            ->options(fn () => Article::query()
                                ->orderBy('id')
                                ->get()
                                ->mapWithKeys(fn (Article $a) => [$a->id => $a->title])
                                ->all())
                            ->searchable()
                            ->required(),
                    ])
                    ->minItems(3)
                    ->maxItems(3)
                    ->defaultItems(3)
                    ->reorderable()
                    ->addActionLabel(trans('content.blocks.articles.add_item')),
            ]);
    }

    protected static function commonFields(): array
    {
        return [
            Toggle::make('is_visible')
                ->label(trans('content.blocks.fields.is_visible'))
                ->default(true),
            TextInput::make('anchor')
                ->label(trans('content.blocks.fields.anchor'))
                ->prefix('#')
                ->alphaDash(),
        ];
    }
}
```

- [ ] **Step 2: Коммит**

```bash
git add app/Filament/Resources/Pages/Schemas/Blocks/ArticlesBlock.php
git commit -m "feat(homepage): ArticlesBlock header + fixed 3 items"
```

---

## Task 11: Регистрация всех 8 блоков в `PageForm`

**Files:**
- Modify: `app/Filament/Resources/Pages/Schemas/PageForm.php`

В `PageForm::mainTab()` находим `Builder::make('blocks')->blocks([...])` и расширяем список до 8 в нужном порядке.

- [ ] **Step 1: Открыть `app/Filament/Resources/Pages/Schemas/PageForm.php` и заменить блок `Builder::make('blocks')`**

Старый блок (строки 80-94 примерно):

```php
Builder::make('blocks')
    ->label(fn () => trans('content.fields.blocks'))
    ->visible(fn (callable $get) => $get('template') === PageTemplate::Landing->value)
    ->blocks([
        HeroBlock::make(),
        ProductsBlock::make(),
        TextBlock::make(),
        ArticlesBlock::make(),
    ])
    ->collapsible()
    ->collapsed()
    ->blockNumbers(false)
    ->addActionLabel(fn () => trans('content.fields.add_block'))
    ->reorderableWithButtons()
    ->cloneable(),
```

Заменить на:

```php
Builder::make('blocks')
    ->label(fn () => trans('content.fields.blocks'))
    ->visible(fn (callable $get) => $get('template') === PageTemplate::Landing->value)
    ->blocks([
        HeroBlock::make(),
        TextBlock::make(),
        ProductsBlock::make(),
        BrandStoryBlock::make(),
        SeriesDuoBlock::make(),
        PillarsBlock::make(),
        TestimonialsBlock::make(),
        ArticlesBlock::make(),
    ])
    ->collapsible()
    ->collapsed()
    ->blockNumbers(false)
    ->addActionLabel(fn () => trans('content.fields.add_block'))
    ->reorderableWithButtons()
    ->cloneable(),
```

- [ ] **Step 2: Добавить use-statements в начало файла**

В `use ...` секции находим существующие импорты блоков и добавляем 4 новых. Итоговый набор импортов:

```php
use App\Filament\Resources\Pages\Schemas\Blocks\ArticlesBlock;
use App\Filament\Resources\Pages\Schemas\Blocks\BrandStoryBlock;
use App\Filament\Resources\Pages\Schemas\Blocks\HeroBlock;
use App\Filament\Resources\Pages\Schemas\Blocks\PillarsBlock;
use App\Filament\Resources\Pages\Schemas\Blocks\ProductsBlock;
use App\Filament\Resources\Pages\Schemas\Blocks\SeriesDuoBlock;
use App\Filament\Resources\Pages\Schemas\Blocks\TestimonialsBlock;
use App\Filament\Resources\Pages\Schemas\Blocks\TextBlock;
```

- [ ] **Step 3: Прогон существующих Filament-тестов**

Run: `php artisan test --filter='PageBuilder' -v`
Expected: PASS (старые тесты на PageBuilder ничего не знают про новые блоки, но компиляция формы должна пройти).

- [ ] **Step 4: Коммит**

```bash
git add app/Filament/Resources/Pages/Schemas/PageForm.php
git commit -m "feat(homepage): register 8 block types in PageForm Builder"
```

---

## Task 12: Blade-компонент `<x-site.pillar>` (атом)

**Files:**
- Create: `resources/views/components/site/pillar.blade.php`

Универсальный атом для одной «точки» в `brand_story` (gem + label + caption) и одного «шага» в `pillars` (eyebrow + title + body). Через `:variant` управляется markup и классы.

- [ ] **Step 1: Создать компонент**

```blade
@props([
    'variant' => 'brand_story', // 'brand_story' | 'pillars'
    'eyebrow' => null,
    'label' => null,
    'caption' => null,
    'title' => null,
    'body' => null,
])

@if($variant === 'brand_story')
    <div class="pt">
        <div class="gem" aria-hidden="true"></div>
        @if($label)<div class="l">{{ $label }}</div>@endif
        @if($caption)<div class="b">{{ $caption }}</div>@endif
    </div>
@elseif($variant === 'pillars')
    <div class="step">
        <div class="deco" aria-hidden="true"></div>
        @if($eyebrow)<div class="num">{{ $eyebrow }}</div>@endif
        @if($title)<h3>{{ $title }}</h3>@endif
        @if($body)<p>{!! nl2br(e($body)) !!}</p>@endif
    </div>
@endif
```

- [ ] **Step 2: Коммит**

```bash
git add resources/views/components/site/pillar.blade.php
git commit -m "feat(homepage): pillar atom component"
```

---

## Task 13: Blade-компонент `<x-site.article-card>`

**Files:**
- Create: `resources/views/components/site/article-card.blade.php`

Карточка статьи для `articles`-блока: cover (img или placeholder) + meta (tag · date · read-time) + h3 + intro/excerpt. Read-time — unicode-safe через `preg_match_all`. URL — `route('articles.show', ['slug' => $article->getTranslation('slug', app()->getLocale())])`. Если маршрут `articles.show` ещё не зарегистрирован — оставить `#`, чтобы Blade не падал.

- [ ] **Step 1: Проверить, есть ли маршрут `articles.show`**

Run: `php artisan route:list | grep articles`
Note: Если есть `articles.show` — используем его. Если нет — задаём ссылку на `#` и помечаем в верификации, что блог-маршруты будут отдельной задачей.

- [ ] **Step 2: Создать компонент**

```blade
@props(['article'])

@php
    /** @var \App\Models\Content\Article $article */
    $locale = app()->getLocale();
    $title = $article->getTranslation('title', $locale) ?: ($article->getTranslation('title', 'uk') ?? '');
    $intro = $article->getTranslation('intro', $locale) ?: ($article->getTranslation('intro', 'uk') ?? '');
    $imgUrl = $article->getFirstMediaUrl('primary', 'card') ?: null;
    $date = $article->published_at?->format('d M Y');

    $contentHtml = $article->getTranslation('content', $locale) ?: ($article->getTranslation('content', 'uk') ?? '');
    $words = preg_match_all('/[\p{L}\p{N}\']+/u', strip_tags($contentHtml));
    $readMin = max(1, (int) ceil(($words ?: 0) / 200));

    $url = \Illuminate\Support\Facades\Route::has('articles.show')
        ? route('articles.show', ['slug' => $article->getTranslation('slug', $locale) ?: $article->getTranslation('slug', 'uk')])
        : '#';
@endphp

<a href="{{ $url }}" class="article-card">
    <div class="cover">
        @if($imgUrl)
            <img src="{{ $imgUrl }}" alt="{{ $title }}" loading="lazy" width="600" height="450">
        @else
            <div class="placeholder" aria-hidden="true">L</div>
        @endif
    </div>
    <div class="meta">
        @if($date)<span>{{ $date }}</span>@endif
        <span>{{ $readMin }} {{ __('site.article.read_min') }}</span>
    </div>
    @if($title)<h3>{{ $title }}</h3>@endif
    @if($intro)<p>{{ \Illuminate\Support\Str::limit($intro, 140) }}</p>@endif
</a>
```

- [ ] **Step 3: Добавить ключ `site.article.read_min` в lang-файлы**

`lang/uk/site.php` — добавить в конец массива:

```php
'article' => [
    'read_min' => 'хв',
],
```

`lang/en/site.php` — то же:

```php
'article' => [
    'read_min' => 'min',
],
```

- [ ] **Step 4: Коммит**

```bash
git add resources/views/components/site/article-card.blade.php lang/uk/site.php lang/en/site.php
git commit -m "feat(homepage): article-card component with unicode-safe read time"
```

---

## Task 14: Blade-компонент `<x-site.review-card>`

**Files:**
- Create: `resources/views/components/site/review-card.blade.php`

Карточка отзыва (gold `“`, italic serif text, meta name · city + stars).

- [ ] **Step 1: Создать компонент**

```blade
@props(['item'])

@php
    $locale = app()->getLocale();
    $tr = function (string $key) use ($item, $locale) {
        $value = $item[$key][$locale] ?? null;
        return filled($value) ? $value : ($item[$key]['uk'] ?? '');
    };
    $quote  = $tr('quote');
    $author = $item['author'] ?? '';
    $city   = $tr('city');
    $rating = (int) ($item['rating'] ?? 0);
@endphp

<article class="review">
    <div class="quote-mark" aria-hidden="true">“</div>
    @if($quote)<p class="text">{{ $quote }}</p>@endif
    <div class="meta">
        <span>
            {{ $author }}@if($city) · {{ $city }}@endif
        </span>
        @if($rating > 0)
            <span class="stars" aria-label="{{ $rating }}/5">
                @for($i = 1; $i <= 5; $i++)
                    {{ $i <= $rating ? '★' : '☆' }}
                @endfor
            </span>
        @endif
    </div>
</article>
```

- [ ] **Step 2: Коммит**

```bash
git add resources/views/components/site/review-card.blade.php
git commit -m "feat(homepage): review-card component"
```

---

## Task 15: Партиал `pages/blocks/hero.blade.php` (переписать)

**Files:**
- Modify: `resources/views/pages/blocks/hero.blade.php`

Editorial split: floating label (абсолют, верх), grid {image | copy} (1.05fr 1.2fr), copy = eyebrow + h1 (top + italic gold bottom) + lead + ctas + meta (3-up).

- [ ] **Step 1: Полностью заменить файл**

```blade
@php
    $locale = app()->getLocale();
    $t = function (string $key) use ($data, $locale) {
        $value = $data[$key][$locale] ?? null;
        return filled($value) ? $value : ($data[$key]['uk'] ?? '');
    };
@endphp

<section class="hero reveal" @if(! empty($data['anchor'])) id="{{ $data['anchor'] }}" @endif>
    <div class="container">
        @php($floating = $t('floating_label'))
        @if($floating)
            <div class="floating">{{ $floating }}</div>
        @endif

        <div class="grid">
            @php($imagePath = $data['image_path'] ?? null)
            <div class="image-wrap">
                @if($imagePath)
                    <img src="{{ \Illuminate\Support\Facades\Storage::disk('public')->url($imagePath) }}" alt="" loading="eager">
                @endif
            </div>

            <div class="copy">
                @php($eyebrow = $t('eyebrow'))
                @if($eyebrow)<div class="eyebrow">{{ $eyebrow }}</div>@endif

                @php($titleTop = $t('title_top'))
                @php($titleBottom = $t('title_bottom'))
                @if($titleTop || $titleBottom)
                    <h1>
                        @if($titleTop){{ $titleTop }}@endif
                        @if($titleBottom)
                            <br><span class="ital">{{ $titleBottom }}</span>
                        @endif
                    </h1>
                @endif

                @php($lead = $t('lead'))
                @if($lead)<p class="lead">{{ $lead }}</p>@endif

                @php($ctaLabel = $t('cta_label'))
                @php($ctaUrl = $data['cta_url'] ?? null)
                @php($secondaryCtaLabel = $t('secondary_cta_label'))
                @php($secondaryCtaUrl = $data['secondary_cta_url'] ?? null)
                @if(($ctaLabel && $ctaUrl) || ($secondaryCtaLabel && $secondaryCtaUrl))
                    <div class="ctas">
                        @if($ctaLabel && $ctaUrl)
                            <a href="{{ $ctaUrl }}" class="btn"><span>{{ $ctaLabel }}</span> <span class="btn-arrow">→</span></a>
                        @endif
                        @if($secondaryCtaLabel && $secondaryCtaUrl)
                            <a href="{{ $secondaryCtaUrl }}" class="btn ghost"><span>{{ $secondaryCtaLabel }}</span></a>
                        @endif
                    </div>
                @endif

                @php($meta = $data['meta'] ?? [])
                @if(! empty($meta))
                    <div class="meta">
                        @foreach($meta as $item)
                            @php($num = $item['num'] ?? '')
                            @php($lbl = ($item['meta_label'][$locale] ?? null) ?: ($item['meta_label']['uk'] ?? ''))
                            <div class="item">
                                <div class="num">{{ $num }}</div>
                                <div class="lbl">{{ $lbl }}</div>
                            </div>
                        @endforeach
                    </div>
                @endif
            </div>
        </div>
    </div>
</section>
```

- [ ] **Step 2: Коммит** (без CSS — CSS добавим в Task 22)

```bash
git add resources/views/pages/blocks/hero.blade.php
git commit -m "feat(homepage): hero partial — editorial split markup"
```

---

## Task 16: Партиал `pages/blocks/text.blade.php` (manifesto)

**Files:**
- Modify: `resources/views/pages/blocks/text.blade.php`

2-col grid: слева eyebrow + h2 (italic, с декоративным `“`), справа body (параграфы, разделены `\n\n` → `<p>`) + опциональная signature.

- [ ] **Step 1: Полностью заменить файл**

```blade
@php
    $locale = app()->getLocale();
    $t = function (string $key) use ($data, $locale) {
        $value = $data[$key][$locale] ?? null;
        return filled($value) ? $value : ($data[$key]['uk'] ?? '');
    };
    $eyebrow = $t('eyebrow');
    $title = $t('title');
    $body = $t('body');
    $signature = $t('signature');
    $paragraphs = collect(preg_split("/\r?\n\r?\n/", trim($body)))->filter()->values();
@endphp

<section class="manifesto reveal" @if(! empty($data['anchor'])) id="{{ $data['anchor'] }}" @endif>
    <div class="container">
        <div class="grid">
            <div>
                @if($eyebrow)<div class="eyebrow">{{ $eyebrow }}</div>@endif
                @if($title)
                    <h2 style="margin-top: 16px;">
                        <span class="quote-open" aria-hidden="true">“</span>
                        {{ $title }}
                    </h2>
                @endif
            </div>
            <div class="body">
                @foreach($paragraphs as $p)
                    <p>{!! nl2br(e($p)) !!}</p>
                @endforeach
                @if($signature)
                    <p style="font-family: var(--font-serif); font-style: italic; color: var(--accent); margin-top: 28px;">{{ $signature }}</p>
                @endif
            </div>
        </div>
    </div>
</section>
```

- [ ] **Step 2: Коммит**

```bash
git add resources/views/pages/blocks/text.blade.php
git commit -m "feat(homepage): text partial — manifesto two-column markup"
```

---

## Task 17: Партиал `pages/blocks/products.blade.php` (slider)

**Files:**
- Modify: `resources/views/pages/blocks/products.blade.php`

Дублируем разметку существующего `<x-site.product-slider>` (там она заточена под локализованные ключи на product page; нам нужны редакторские поля). Шапка eyebrow + h2 + cta-lnk, под ней track из `<x-site.product-card>`.

- [ ] **Step 1: Полностью заменить файл**

```blade
@php
    $locale = app()->getLocale();
    $t = function (string $key) use ($data, $locale) {
        $value = $data[$key][$locale] ?? null;
        return filled($value) ? $value : ($data[$key]['uk'] ?? '');
    };

    $ids = collect($data['items'] ?? [])->pluck('product_id')->filter()->all();
    $products = $ids
        ? \App\Models\Catalogue\Product::query()->whereIn('id', $ids)->get()->keyBy('id')
        : collect();

    $eyebrow = $t('eyebrow');
    $title = $t('title');
    $ctaLabel = $t('cta_label');
    $ctaUrl = $data['cta_url'] ?? null;
@endphp

@if($products->isNotEmpty())
    <section class="product-slider reveal" @if(! empty($data['anchor'])) id="{{ $data['anchor'] }}" @endif>
        <div class="container">
            <div class="head">
                @if($eyebrow)<div class="eyebrow">{{ $eyebrow }}</div>@endif
                @if($title)<h2>{{ $title }}</h2>@endif
                @if($ctaLabel && $ctaUrl)
                    <a href="{{ $ctaUrl }}" class="lnk">{{ $ctaLabel }} →</a>
                @endif
            </div>
            <div class="track">
                @foreach($ids as $id)
                    @if($product = $products[$id] ?? null)
                        <x-site.product-card :product="$product" />
                    @endif
                @endforeach
            </div>
        </div>
    </section>
@endif
```

- [ ] **Step 2: Коммит**

```bash
git add resources/views/pages/blocks/products.blade.php
git commit -m "feat(homepage): products partial — slider with editorial header"
```

---

## Task 18: Партиал `pages/blocks/brand_story.blade.php` (ThreePoints)

**Files:**
- Create: `resources/views/pages/blocks/brand_story.blade.php`

Centered head (eyebrow + h2 italic + body lead) + `.points` grid (1fr auto 1fr auto 1fr): 3 `<x-site.pillar variant="brand_story">` с `.conn` между ними.

- [ ] **Step 1: Создать файл**

```blade
@php
    $locale = app()->getLocale();
    $t = function (string $key) use ($data, $locale) {
        $value = $data[$key][$locale] ?? null;
        return filled($value) ? $value : ($data[$key]['uk'] ?? '');
    };
    $eyebrow = $t('eyebrow');
    $title = $t('title');
    $body = $t('body');
    $pillars = $data['pillars'] ?? [];
@endphp

<section class="threepoints reveal" @if(! empty($data['anchor'])) id="{{ $data['anchor'] }}" @endif>
    <div class="container">
        <div class="head">
            @if($eyebrow)<div class="eyebrow">{{ $eyebrow }}</div>@endif
            @if($title)<h2>{{ $title }}</h2>@endif
            @if($body)<p>{{ $body }}</p>@endif
        </div>

        @if(! empty($pillars))
            <div class="points">
                @foreach($pillars as $i => $pillar)
                    @php
                        $label = ($pillar['pillar_label'][$locale] ?? null) ?: ($pillar['pillar_label']['uk'] ?? '');
                        $caption = ($pillar['pillar_caption'][$locale] ?? null) ?: ($pillar['pillar_caption']['uk'] ?? '');
                    @endphp
                    <x-site.pillar variant="brand_story" :label="$label" :caption="$caption" />
                    @if($i < count($pillars) - 1)
                        <div class="conn" aria-hidden="true"></div>
                    @endif
                @endforeach
            </div>
        @endif
    </div>
</section>
```

- [ ] **Step 2: Коммит**

```bash
git add resources/views/pages/blocks/brand_story.blade.php
git commit -m "feat(homepage): brand_story partial (three-points)"
```

---

## Task 19: Партиал `pages/blocks/series_duo.blade.php` (Collections)

**Files:**
- Create: `resources/views/pages/blocks/series_duo.blade.php`

Section head (eyebrow + h2). Grid 1fr 1fr из 2 `.collection-card`. Каждая карточка: фоновый img + overlay + kicker/title + description/arrow-cta. Safe Series lookup для `cta_url`.

- [ ] **Step 1: Создать файл**

```blade
@php
    $locale = app()->getLocale();
    $t = function (string $key) use ($data, $locale) {
        $value = $data[$key][$locale] ?? null;
        return filled($value) ? $value : ($data[$key]['uk'] ?? '');
    };
    $eyebrow = $t('eyebrow');
    $title = $t('title');
    $items = $data['items'] ?? [];
@endphp

<section class="collections reveal" @if(! empty($data['anchor'])) id="{{ $data['anchor'] }}" @endif>
    <div class="container">
        <div class="section-head">
            <div>
                @if($eyebrow)<div class="eyebrow">{{ $eyebrow }}</div>@endif
                @if($title)<h2 style="margin-top: 12px;">{{ $title }}</h2>@endif
            </div>
        </div>

        @if(! empty($items))
            <div class="grid">
                @foreach($items as $item)
                    @php
                        $tr = function (string $key) use ($item, $locale) {
                            $value = $item[$key][$locale] ?? null;
                            return filled($value) ? $value : ($item[$key]['uk'] ?? '');
                        };
                        $kicker = $tr('kicker');
                        $itemTitle = $tr('title');
                        $description = $tr('description');
                        $ctaLabel = $tr('cta_label');
                        $imagePath = $item['image_path'] ?? null;

                        $series = ! empty($item['series_id'])
                            ? \App\Models\Catalogue\Series::find($item['series_id'])
                            : null;
                        $ctaUrl = $series
                            ? route('products.index', ['series' => $series->slug])
                            : route('products.index');
                        $displayTitle = $itemTitle !== '' ? $itemTitle : ($series?->name ?? '');
                    @endphp

                    <a href="{{ $ctaUrl }}" class="collection-card">
                        @if($imagePath)
                            <img src="{{ \Illuminate\Support\Facades\Storage::disk('public')->url($imagePath) }}" alt="{{ $displayTitle }}">
                        @endif
                        <div class="overlay" aria-hidden="true"></div>
                        <div>
                            @if($kicker)<div class="lbl">{{ $kicker }}</div>@endif
                            @if($displayTitle)<div class="name" style="margin-top: 8px;">{{ $displayTitle }}</div>@endif
                        </div>
                        <div>
                            @if($description)<div class="desc">{{ $description }}</div>@endif
                            @if($ctaLabel)
                                <div class="arrow" style="margin-top: 24px;">{{ $ctaLabel }} →</div>
                            @endif
                        </div>
                    </a>
                @endforeach
            </div>
        @endif
    </div>
</section>
```

- [ ] **Step 2: Коммит**

```bash
git add resources/views/pages/blocks/series_duo.blade.php
git commit -m "feat(homepage): series_duo partial (collections) with safe Series lookup"
```

---

## Task 20: Партиал `pages/blocks/pillars.blade.php` (Guide + Advantages)

**Files:**
- Create: `resources/views/pages/blocks/pillars.blade.php`

Section head + grid из N `<x-site.pillar variant="pillars">`. Класс `.pillars.is-tinted` если `surface=tinted`. Атрибут `data-count="3|4"` для CSS-разделения 3-col (Guide) vs 4-col с дивайдерами (Advantages).

- [ ] **Step 1: Создать файл**

```blade
@php
    $locale = app()->getLocale();
    $t = function (string $key) use ($data, $locale) {
        $value = $data[$key][$locale] ?? null;
        return filled($value) ? $value : ($data[$key]['uk'] ?? '');
    };
    $eyebrow = $t('eyebrow');
    $title = $t('title');
    $body = $t('body');
    $items = $data['items'] ?? [];
    $surface = ($data['surface'] ?? 'default') === 'tinted' ? 'is-tinted' : '';
    $count = count($items);
    $sectionClass = trim("pillars reveal {$surface}");
@endphp

<section class="{{ $sectionClass }}" data-count="{{ $count }}" @if(! empty($data['anchor'])) id="{{ $data['anchor'] }}" @endif>
    <div class="container">
        <div class="section-head">
            <div>
                @if($eyebrow)<div class="eyebrow">{{ $eyebrow }}</div>@endif
                @if($title)<h2 style="margin-top: 12px;">{{ $title }}</h2>@endif
                @if($body)<p style="margin-top: 16px; max-width: 60ch; color: var(--ink-soft);">{{ $body }}</p>@endif
            </div>
        </div>

        @if(! empty($items))
            <div class="grid">
                @foreach($items as $i => $item)
                    @php
                        $itemEyebrow = ($item['eyebrow'][$locale] ?? null) ?: ($item['eyebrow']['uk'] ?? '');
                        $itemTitle = ($item['title'][$locale] ?? null) ?: ($item['title']['uk'] ?? '');
                        $itemBody = ($item['body'][$locale] ?? null) ?: ($item['body']['uk'] ?? '');
                        // Auto-number eyebrow if editor left it blank
                        if ($itemEyebrow === '') {
                            $itemEyebrow = str_pad((string) ($i + 1), 2, '0', STR_PAD_LEFT);
                        }
                    @endphp
                    <x-site.pillar
                        variant="pillars"
                        :eyebrow="$itemEyebrow"
                        :title="$itemTitle"
                        :body="$itemBody"
                    />
                @endforeach
            </div>
        @endif
    </div>
</section>
```

- [ ] **Step 2: Коммит**

```bash
git add resources/views/pages/blocks/pillars.blade.php
git commit -m "feat(homepage): pillars partial (Guide + Advantages with surface variant)"
```

---

## Task 21: Партиал `pages/blocks/testimonials.blade.php` (slider)

**Files:**
- Create: `resources/views/pages/blocks/testimonials.blade.php`

Section head + horizontal track из `<x-site.review-card>`. Track ведёт себя как `.product-slider .track` (scroll-snap, no scrollbar, edge-bleed).

- [ ] **Step 1: Создать файл**

```blade
@php
    $locale = app()->getLocale();
    $t = function (string $key) use ($data, $locale) {
        $value = $data[$key][$locale] ?? null;
        return filled($value) ? $value : ($data[$key]['uk'] ?? '');
    };
    $eyebrow = $t('eyebrow');
    $title = $t('title');
    $ctaLabel = $t('cta_label');
    $ctaUrl = $data['cta_url'] ?? null;
    $items = $data['items'] ?? [];
@endphp

<section class="testimonials reveal" @if(! empty($data['anchor'])) id="{{ $data['anchor'] }}" @endif>
    <div class="container">
        <div class="section-head">
            <div>
                @if($eyebrow)<div class="eyebrow">{{ $eyebrow }}</div>@endif
                @if($title)<h2 style="margin-top: 12px;">{{ $title }}</h2>@endif
            </div>
            @if($ctaLabel && $ctaUrl)
                <a href="{{ $ctaUrl }}" class="lnk">{{ $ctaLabel }} →</a>
            @endif
        </div>

        @if(! empty($items))
            <div class="track">
                @foreach($items as $item)
                    <x-site.review-card :item="$item" />
                @endforeach
            </div>
        @endif
    </div>
</section>
```

- [ ] **Step 2: Коммит**

```bash
git add resources/views/pages/blocks/testimonials.blade.php
git commit -m "feat(homepage): testimonials partial (reviews slider)"
```

---

## Task 22: Партиал `pages/blocks/articles.blade.php` (grid 3)

**Files:**
- Modify: `resources/views/pages/blocks/articles.blade.php`

Section head (с CTA-link справа) + grid 3 из `<x-site.article-card>`. Жёстко режем массив до первых 3 элементов (даже если в JSON их 4 — защита от ручного редактирования).

- [ ] **Step 1: Полностью заменить файл**

```blade
@php
    $locale = app()->getLocale();
    $t = function (string $key) use ($data, $locale) {
        $value = $data[$key][$locale] ?? null;
        return filled($value) ? $value : ($data[$key]['uk'] ?? '');
    };

    $ids = collect($data['items'] ?? [])->pluck('article_id')->filter()->take(3)->all();
    $articles = $ids
        ? \App\Models\Content\Article::query()->whereIn('id', $ids)->get()->keyBy('id')
        : collect();

    $eyebrow = $t('eyebrow');
    $title = $t('title');
    $ctaLabel = $t('cta_label');
    $ctaUrl = $data['cta_url'] ?? null;
@endphp

@if($articles->isNotEmpty())
    <section class="blog reveal" @if(! empty($data['anchor'])) id="{{ $data['anchor'] }}" @endif>
        <div class="container">
            <div class="section-head">
                <div>
                    @if($eyebrow)<div class="eyebrow">{{ $eyebrow }}</div>@endif
                    @if($title)<h2 style="margin-top: 12px;">{{ $title }}</h2>@endif
                </div>
                @if($ctaLabel && $ctaUrl)
                    <a href="{{ $ctaUrl }}" class="lnk">{{ $ctaLabel }} →</a>
                @endif
            </div>

            <div class="grid">
                @foreach($ids as $id)
                    @if($article = $articles[$id] ?? null)
                        <x-site.article-card :article="$article" />
                    @endif
                @endforeach
            </div>
        </div>
    </section>
@endif
```

- [ ] **Step 2: Коммит**

```bash
git add resources/views/pages/blocks/articles.blade.php
git commit -m "feat(homepage): articles partial — grid of 3 cards"
```

---

## Task 23: CSS — `components/hero.css`

**Files:**
- Create: `resources/css/site/components/hero.css`

Адаптация `.hero*` из эталона (styles.css строки 329-375). Токены без изменений — наш `theme-cream` совпадает с эталоном.

- [ ] **Step 1: Создать файл**

```css
/* Hero — editorial split with image left, copy right. Floating tag absolutely positioned. */

.hero {
  padding: 80px 0 120px;
  position: relative;
  overflow: hidden;
}
.hero .grid {
  display: grid; grid-template-columns: 1.05fr 1.2fr; gap: 80px;
  align-items: center;
}
.hero .image-wrap {
  position: relative; aspect-ratio: 4/5;
  overflow: hidden;
}
.hero .image-wrap img {
  width: 100%; height: 100%; object-fit: cover;
  transition: transform 1.2s var(--ease-out);
  will-change: transform;
}
.hero .image-wrap:hover img { transform: scale(1.03); }
.hero .image-wrap::after {
  content: ""; position: absolute; inset: 0; pointer-events: none;
  border: 1px solid var(--line);
}
.hero .copy h1 .ital { font-style: italic; color: var(--accent); }
.hero .copy .lead { margin-top: 32px; }
.hero .copy .ctas { display: flex; gap: 16px; margin-top: 40px; flex-wrap: wrap; }
.hero .meta {
  margin-top: 64px; display: grid; grid-template-columns: repeat(3, 1fr); gap: 32px;
  border-top: 1px solid var(--line-soft); padding-top: 32px;
}
.hero .meta .item .num { font-family: var(--font-serif); font-size: 36px; color: var(--ink); }
.hero .meta .item .lbl { font-size: 11px; letter-spacing: 0.18em; text-transform: uppercase; color: var(--ink-mute); margin-top: 6px; }

.hero .floating {
  position: absolute; top: 60px; right: 56px;
  font-family: var(--font-serif); font-style: italic; font-size: 14px; color: var(--ink-mute);
  max-width: 25ch; text-align: right;
}

@media (max-width: 900px) {
  .hero .grid { grid-template-columns: 1fr; gap: 40px; }
  .hero .floating { display: none; }
}
```

- [ ] **Step 2: Коммит**

```bash
git add resources/css/site/components/hero.css
git commit -m "feat(homepage): hero component styles"
```

---

## Task 24: CSS — `components/manifesto.css`

**Files:**
- Create: `resources/css/site/components/manifesto.css`

Адаптация `.manifesto*` (эталон 378-388).

- [ ] **Step 1: Создать файл**

```css
/* Manifesto — two-column editorial with italic quote and signature. Tinted bg-2. */

.manifesto { background: var(--bg-2); }
.manifesto .container {
  /* Уже есть section { padding: 120px 0; } в layout.css — фон распространяется на всю секцию. */
}
.manifesto .grid {
  display: grid; grid-template-columns: 1fr 1.4fr; gap: 80px;
  align-items: start;
}
.manifesto h2 {
  font-style: italic;
  font-size: clamp(36px, 4vw, 64px);
}
.manifesto .body {
  color: var(--ink-soft);
  font-size: 17px; line-height: 1.7;
  max-width: 56ch;
}
.manifesto .body p + p { margin-top: 18px; }
.manifesto .body .quote-open {
  font-family: var(--font-serif); font-style: italic; color: var(--accent);
  font-size: 96px; line-height: 0; display: block; height: 36px;
}

@media (max-width: 800px) {
  .manifesto .grid { grid-template-columns: 1fr; gap: 32px; }
}
```

- [ ] **Step 2: Коммит**

```bash
git add resources/css/site/components/manifesto.css
git commit -m "feat(homepage): manifesto component styles"
```

---

## Task 25: CSS — `components/three-points.css`

**Files:**
- Create: `resources/css/site/components/three-points.css`

Эталон 390-432.

- [ ] **Step 1: Создать файл**

```css
/* Three-points (brand_story) — centered head + 3 gem-marked points with golden connectors. */

.threepoints {
  padding: 100px 0;
  background: var(--bg);
}
.threepoints .head {
  text-align: center;
  max-width: 64ch;
  margin: 0 auto 64px;
}
.threepoints .head h2 { font-style: italic; margin-top: 18px; }
.threepoints .head p {
  color: var(--ink-soft);
  margin-top: 24px;
  font-size: 17px; line-height: 1.7;
}
.threepoints .points {
  display: grid; grid-template-columns: 1fr auto 1fr auto 1fr; gap: 32px;
  align-items: stretch;
  max-width: 1080px; margin: 0 auto;
}
.threepoints .pt {
  display: flex; flex-direction: column; align-items: center; gap: 14px;
  padding: 32px 16px;
}
.threepoints .pt .gem {
  width: 18px; height: 18px; transform: rotate(45deg);
  border: 1px solid var(--accent); background: transparent;
  margin-bottom: 14px;
  position: relative;
}
.threepoints .pt .gem::after {
  content: ""; position: absolute; inset: 4px;
  background: var(--accent);
}
.threepoints .pt .l {
  font-family: var(--font-serif);
  font-size: clamp(28px, 2.4vw, 36px);
  color: var(--ink);
  font-style: italic;
}
.threepoints .pt .b {
  font-size: 11px; letter-spacing: 0.24em; text-transform: uppercase;
  color: var(--ink-mute);
}
.threepoints .conn {
  align-self: center;
  width: 60px; height: 1px;
  background: linear-gradient(90deg, transparent, var(--accent) 30%, var(--accent) 70%, transparent);
  position: relative;
}
.threepoints .conn::after {
  content: ""; position: absolute;
  top: 50%; right: -5px;
  transform: translateY(-50%) rotate(45deg);
  width: 6px; height: 6px;
  border-top: 1px solid var(--accent);
  border-right: 1px solid var(--accent);
}

@media (max-width: 800px) {
  .threepoints .points { grid-template-columns: 1fr; }
  .threepoints .conn {
    width: 1px; height: 40px;
    background: linear-gradient(180deg, transparent, var(--accent), transparent);
    justify-self: center;
  }
  .threepoints .conn::after { display: none; }
}
```

- [ ] **Step 2: Коммит**

```bash
git add resources/css/site/components/three-points.css
git commit -m "feat(homepage): three-points (brand_story) component styles"
```

---

## Task 26: CSS — `components/collections.css`

**Files:**
- Create: `resources/css/site/components/collections.css`

Эталон 491-516.

- [ ] **Step 1: Создать файл**

```css
/* Collections (series_duo) — two image cards with overlay text. */

.collections .grid {
  display: grid; grid-template-columns: 1fr 1fr;
  gap: 24px;
}
.collection-card {
  position: relative; aspect-ratio: 4/5;
  overflow: hidden;
  display: flex; flex-direction: column; justify-content: space-between;
  padding: 40px;
  color: #fff;
  transition: all 0.5s var(--ease-out);
}
.collection-card img {
  position: absolute; inset: 0;
  width: 100%; height: 100%; object-fit: cover;
  z-index: 0;
  transition: transform 1.2s var(--ease-out);
}
.collection-card:hover img { transform: scale(1.04); }
.collection-card .overlay {
  position: absolute; inset: 0;
  background: linear-gradient(180deg, rgba(0,0,0,0.05) 0%, rgba(0,0,0,0.65) 100%);
  z-index: 1;
}
.collection-card > * { position: relative; z-index: 2; }
.collection-card .lbl {
  font-size: 10px; letter-spacing: 0.32em; text-transform: uppercase;
  color: rgba(255, 250, 240, 0.8);
}
.collection-card .name {
  font-family: var(--font-serif);
  font-size: clamp(36px, 3.5vw, 56px);
  line-height: 1;
}
.collection-card .desc {
  color: rgba(255, 250, 240, 0.85);
  max-width: 36ch;
  font-size: 15px; line-height: 1.55;
}
.collection-card .arrow {
  display: inline-flex; align-items: center; gap: 12px;
  font-size: 11px; letter-spacing: 0.22em; text-transform: uppercase;
  padding-bottom: 8px;
  border-bottom: 1px solid rgba(255, 250, 240, 0.4);
  width: max-content;
  transition: all var(--t-med) var(--ease-out);
}
.collection-card:hover .arrow {
  border-bottom-color: var(--accent-2);
  gap: 18px;
}

@media (max-width: 800px) {
  .collections .grid { grid-template-columns: 1fr; }
}
```

- [ ] **Step 2: Коммит**

```bash
git add resources/css/site/components/collections.css
git commit -m "feat(homepage): collections (series_duo) component styles"
```

---

## Task 27: CSS — `components/pillars.css`

**Files:**
- Create: `resources/css/site/components/pillars.css`

Объединяет `.guide` (3-col + tinted) и `.advantages` (3-4-col + divider border) из эталона 518-553. Базовый `.pillars` — grid 3-col + step-card (как `.guide .step`). Модификатор `.is-tinted` → `background: var(--bg-2)`. Селектор `[data-count="4"]` → 4-col + дивайдеры (как `.advantages`).

- [ ] **Step 1: Создать файл**

```css
/* Pillars — Guide-style numbered editorial. Default = 3-col cards; data-count=4 = 4-col with dividers. */

.pillars {
  padding: 120px 0;
  background: var(--bg);
}
.pillars.is-tinted { background: var(--bg-2); }

.pillars .grid {
  display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 24px;
  margin-top: 32px;
}
.pillars .step {
  background: var(--card);
  border: 1px solid var(--line-soft);
  padding: 40px;
  display: flex; flex-direction: column; gap: 16px;
  position: relative;
  min-height: 280px;
  transition: all var(--t-med) var(--ease-out);
}
.pillars .step:hover {
  border-color: var(--accent);
  transform: translateY(-4px);
}
.pillars .step .num {
  font-family: var(--font-serif);
  font-size: 14px; color: var(--accent);
  letter-spacing: 0.2em;
}
.pillars .step h3 { font-style: italic; }
.pillars .step p { color: var(--ink-soft); }
.pillars .step .deco {
  position: absolute; right: 28px; top: 28px;
  width: 30px; height: 30px;
  border-top: 1px solid var(--accent);
  border-right: 1px solid var(--accent);
  transition: all var(--t-med) var(--ease-out);
}
.pillars .step:hover .deco { width: 40px; height: 40px; }

/* 4-item variant — Advantages style: divider-separated cells, no card chrome. */
.pillars[data-count="4"] .grid {
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 0;
  border-top: 1px solid var(--line-soft);
  border-bottom: 1px solid var(--line-soft);
}
.pillars[data-count="4"] .step {
  background: transparent;
  border: none;
  border-right: 1px solid var(--line-soft);
  padding: 40px 32px;
  min-height: auto;
  transform: none;
}
.pillars[data-count="4"] .step:hover {
  background: var(--bg-2);
  border-color: var(--line-soft);
  transform: none;
}
.pillars[data-count="4"] .step:last-child { border-right: none; }
.pillars[data-count="4"] .step .deco { display: none; }
.pillars[data-count="4"] .step h3 { font-size: 22px; }
.pillars[data-count="4"] .step p { font-size: 14px; line-height: 1.55; }

@media (max-width: 900px) {
  .pillars .grid,
  .pillars[data-count="4"] .grid {
    grid-template-columns: 1fr 1fr; gap: 0;
  }
  .pillars[data-count="4"] .step {
    border-bottom: 1px solid var(--line-soft);
  }
  .pillars[data-count="4"] .step:nth-child(2n) { border-right: none; }
}
@media (max-width: 600px) {
  .pillars .grid,
  .pillars[data-count="4"] .grid {
    grid-template-columns: 1fr;
  }
}
```

- [ ] **Step 2: Коммит**

```bash
git add resources/css/site/components/pillars.css
git commit -m "feat(homepage): pillars styles (Guide 3-col + Advantages 4-col variants)"
```

---

## Task 28: CSS — `components/testimonials.css`

**Files:**
- Create: `resources/css/site/components/testimonials.css`

Эталон 556-568 для `.review`-карточки + slider track аналогично `.product-slider .track`.

- [ ] **Step 1: Создать файл**

```css
/* Testimonials — horizontal slider of review cards. Slider mechanics mirror .product-slider .track. */

.testimonials {
  padding: 120px 0;
  background: var(--bg-2);
}
.testimonials .section-head { margin-bottom: 32px; }

.testimonials .track {
  display: grid;
  grid-auto-flow: column;
  grid-auto-columns: minmax(360px, 1fr);
  gap: 24px;
  overflow-x: auto;
  scroll-snap-type: x mandatory;
  padding-bottom: 12px;
  scrollbar-width: none;
  margin-right: calc(50% - 50vw);
}
.testimonials .track::-webkit-scrollbar { display: none; }
.testimonials .track > * {
  scroll-snap-align: start;
  min-width: 0;
}

/* Review card */
.review {
  background: var(--card);
  border: 1px solid var(--line-soft);
  padding: 40px;
  display: flex; flex-direction: column; gap: 18px;
  transition: all var(--t-med) var(--ease-out);
}
.review:hover { border-color: var(--accent); }
.review .quote-mark {
  font-family: var(--font-serif);
  font-size: 64px; line-height: 0.3;
  color: var(--accent);
  height: 22px;
}
.review .text {
  font-family: var(--font-serif);
  font-size: 22px; line-height: 1.45;
  font-style: italic;
  color: var(--ink);
}
.review .meta {
  font-size: 11px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--ink-mute);
  display: flex; justify-content: space-between; align-items: center;
}
.review .stars {
  color: var(--accent);
  letter-spacing: 0.1em;
}
```

- [ ] **Step 2: Коммит**

```bash
git add resources/css/site/components/testimonials.css
git commit -m "feat(homepage): testimonials slider + review card styles"
```

---

## Task 29: CSS — `components/article-card.css`

**Files:**
- Create: `resources/css/site/components/article-card.css`

Эталон 571-582.

- [ ] **Step 1: Создать файл**

```css
/* Blog section + article card (used in articles partial). */

.blog {
  padding: 120px 0;
  background: var(--bg);
}
.blog .grid {
  display: grid; grid-template-columns: repeat(3, 1fr);
  gap: 24px;
  margin-top: 32px;
}

.article-card {
  display: flex; flex-direction: column; gap: 16px;
}
.article-card .cover {
  aspect-ratio: 4/3;
  overflow: hidden;
  background: var(--bg-2);
  position: relative;
}
.article-card .cover img {
  width: 100%; height: 100%; object-fit: cover;
  transition: transform 0.9s var(--ease-out);
}
.article-card .cover .placeholder {
  position: absolute; inset: 0;
  display: flex; align-items: center; justify-content: center;
  font-family: var(--font-serif); font-size: 40px; letter-spacing: 0.3em;
  color: var(--ink-mute); opacity: 0.5;
}
.article-card:hover .cover img { transform: scale(1.04); }
.article-card .meta {
  font-size: 11px; letter-spacing: 0.18em; text-transform: uppercase;
  color: var(--ink-mute);
  display: flex; gap: 16px;
}
.article-card .meta .tag { color: var(--accent); }
.article-card h3 {
  font-family: var(--font-serif);
  font-size: 22px; line-height: 1.25;
  font-weight: 400;
  transition: color var(--t-fast) var(--ease-out);
}
.article-card:hover h3 { color: var(--accent); }
.article-card p {
  color: var(--ink-soft);
  font-size: 14px; line-height: 1.6;
}

@media (max-width: 900px) {
  .blog .grid { grid-template-columns: 1fr; }
}
```

- [ ] **Step 2: Коммит**

```bash
git add resources/css/site/components/article-card.css
git commit -m "feat(homepage): article-card + blog section styles"
```

---

## Task 30: Регистрация всех новых CSS в `index.css`

**Files:**
- Modify: `resources/css/site/index.css`

Добавить 7 `@import` строк после существующих компонентов.

- [ ] **Step 1: Открыть `resources/css/site/index.css` и добавить импорты**

После строки `@import './components/product-slider.css';` добавить:

```css
@import './components/hero.css';
@import './components/manifesto.css';
@import './components/three-points.css';
@import './components/collections.css';
@import './components/pillars.css';
@import './components/testimonials.css';
@import './components/article-card.css';
```

- [ ] **Step 2: Проверить компиляцию Vite**

Run: `npm run build`
Expected: успешная сборка без ошибок «Cannot resolve...».

- [ ] **Step 3: Коммит**

```bash
git add resources/css/site/index.css
git commit -m "feat(homepage): register 7 new component CSS in site index"
```

---

## Task 31: Реордер сидеров в `DatabaseSeeder`

**Files:**
- Modify: `database/seeders/DatabaseSeeder.php`

Сейчас порядок в `run()` (строки 35-49) такой, что `PageSeeder` идёт перед `ArticleSeeder`. `PageSeeder` (Task 32) будет читать `Article::limit(3)->pluck('id')` — без реордера получит пусто.

- [ ] **Step 1: Заменить блок `$this->call([...])` в `DatabaseSeeder::run()`**

```php
$this->call([
    PerfumeFamilySeeder::class,
    ConcentrationSeeder::class,
    SeasonSeeder::class,
    TagSeeder::class,
    SeriesSeeder::class,
    OccasionSeeder::class,
    AudienceSeeder::class,
    BrandSeeder::class,
    NoteSeeder::class,
    ProductSeeder::class,
    ArticleSeeder::class,
    PageSeeder::class,
    FormSubmissionSeeder::class,
]);
```

Цепочка зависимостей: `ProductSeeder → ArticleSeeder → PageSeeder` (Page нужен и Product, и Article для seeded блоков).

- [ ] **Step 2: Прогон migrate:fresh --seed для проверки порядка**

Run: `php artisan migrate:fresh --seed`
Expected: успешно, без ошибок. (Контент главной мы перепишем в Task 32, сейчас просто убеждаемся что новый порядок не ломает существующее.)

- [ ] **Step 3: Коммит**

```bash
git add database/seeders/DatabaseSeeder.php
git commit -m "fix(seeders): run ArticleSeeder before PageSeeder for homepage articles block"
```

---

## Task 32: Скопировать assets из design-sources в seeders

**Files:**
- Create: `database/seeders/images/levant-luxury-bottle.jpg`
- Create: `database/seeders/images/levant-flacon-3.jpg`
- Create: `database/seeders/images/levant-flacon-4.jpg`

Эти 3 файла нужны `PageSeeder` для hero и series_duo. Источник — `docs/superpowers/design-sources/levant-parfums/project/assets/`.

- [ ] **Step 1: Скопировать файлы**

```bash
cp docs/superpowers/design-sources/levant-parfums/project/assets/levant-luxury-bottle.jpg database/seeders/images/levant-luxury-bottle.jpg
cp docs/superpowers/design-sources/levant-parfums/project/assets/levant-flacon-3.jpg database/seeders/images/levant-flacon-3.jpg
cp docs/superpowers/design-sources/levant-parfums/project/assets/levant-flacon-4.jpg database/seeders/images/levant-flacon-4.jpg
```

- [ ] **Step 2: Убедиться, что файлы скопировались**

```bash
ls -la database/seeders/images/levant-*.jpg
```

Expected: 3 файла существуют, размер > 0.

- [ ] **Step 3: Коммит**

```bash
git add database/seeders/images/levant-luxury-bottle.jpg database/seeders/images/levant-flacon-3.jpg database/seeders/images/levant-flacon-4.jpg
git commit -m "feat(homepage): seed images for hero + series_duo"
```

---

## Task 33: Переписать `PageSeeder` — 10 секций главной

**Files:**
- Modify: `database/seeders/Content/PageSeeder.php`

Главный seeder: копируем 3 ассета в `storage/app/public/pages/blocks/`, выбираем Series/Product/Article, собираем массив `blocks` с дословным копирайтом из `data.js`.

- [ ] **Step 1: Полностью заменить содержимое `database/seeders/Content/PageSeeder.php`**

```php
<?php

namespace Database\Seeders\Content;

use App\Enums\PageTemplate;
use App\Models\Catalogue\Product;
use App\Models\Catalogue\Series;
use App\Models\Content\Article;
use App\Models\Content\Page;
use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\Storage;

class PageSeeder extends Seeder
{
    public function run(): void
    {
        // 1. Copy seed images to public storage (once per fresh).
        $this->copyAsset('levant-luxury-bottle.jpg');
        $this->copyAsset('levant-flacon-3.jpg');
        $this->copyAsset('levant-flacon-4.jpg');

        // 2. Resolve related entities.
        $luxury = Series::where('slug', 'luxury')->first();
        $onyx = Series::where('slug', 'onyx')->first();

        $bestsellers = Product::query()
            ->whereHas('tags', fn ($q) => $q->where('slug', 'bestseller'))
            ->limit(6)
            ->pluck('id')
            ->all();
        if (empty($bestsellers)) {
            $bestsellers = Product::query()->limit(6)->pluck('id')->all();
        }

        $newItems = Product::query()
            ->whereHas('tags', fn ($q) => $q->where('slug', 'new'))
            ->limit(6)
            ->pluck('id')
            ->all();
        if (empty($newItems)) {
            $newItems = Product::query()->orderByDesc('id')->limit(6)->pluck('id')->all();
        }

        $articleIds = Article::query()
            ->orderByDesc('published_at')
            ->limit(3)
            ->pluck('id')
            ->all();

        // 3. Build blocks array.
        $blocks = $this->buildBlocks(
            luxury: $luxury,
            onyx: $onyx,
            bestsellerIds: $bestsellers,
            newIds: $newItems,
            articleIds: $articleIds,
        );

        // 4. Upsert homepage.
        Page::query()->updateOrCreate(
            ['is_homepage' => true],
            [
                'slug' => ['uk' => 'home-uk', 'en' => 'home-en'],
                'title' => ['uk' => 'Головна', 'en' => 'Home'],
                'intro' => ['uk' => '', 'en' => ''],
                'content' => null,
                'seo_title' => [
                    'uk' => 'Levant Parfums · Нішевий аромат. Чесна ціна.',
                    'en' => 'Levant Parfums · Niche fragrance. Honest price.',
                ],
                'seo_description' => [
                    'uk' => 'Levant — 22 композиції у двох колекціях. Розроблено в Іспанії, розлито в Туреччині, зібрано в Україні.',
                    'en' => 'Levant — 22 compositions in two collections. Composed in Spain, bottled in Turkey, assembled in Ukraine.',
                ],
                'is_published' => true,
                'template' => PageTemplate::Landing,
                'blocks' => $blocks,
            ],
        );
    }

    private function copyAsset(string $filename): void
    {
        $target = "pages/blocks/{$filename}";
        if (Storage::disk('public')->exists($target)) {
            return;
        }
        $source = database_path("seeders/images/{$filename}");
        if (! file_exists($source)) {
            return;
        }
        Storage::disk('public')->put($target, file_get_contents($source));
    }

    private function buildBlocks(?Series $luxury, ?Series $onyx, array $bestsellerIds, array $newIds, array $articleIds): array
    {
        return [
            // 1. Hero
            [
                'type' => 'hero',
                'data' => [
                    'is_visible' => true,
                    'eyebrow' => [
                        'uk' => 'Колекція 2026 · Luxury × Onyx',
                        'en' => 'Collection 2026 · Luxury × Onyx',
                    ],
                    'title_top' => [
                        'uk' => 'Нішевий аромат.',
                        'en' => 'Niche fragrance.',
                    ],
                    'title_bottom' => [
                        'uk' => 'Чесна ціна.',
                        'en' => 'Honest price.',
                    ],
                    'lead' => [
                        'uk' => 'Levant — це 22 композиції у двох колекціях: Luxury Series та Onyx Series. Розроблено в Іспанії, розлито в Туреччині, ринок і душа — в Україні. Без переплати за логотип.',
                        'en' => 'Levant is 22 compositions in two collections — Luxury Series and Onyx Series. Composed in Spain, bottled in Turkey, the market and the soul in Ukraine. No premium for a logo.',
                    ],
                    'floating_label' => [
                        'uk' => 'Іспанія → Туреччина → Україна',
                        'en' => 'Spain → Turkey → Ukraine',
                    ],
                    'cta_label' => ['uk' => 'Дослідити каталог', 'en' => 'Explore the catalogue'],
                    'cta_url' => '/products',
                    'secondary_cta_label' => ['uk' => 'Філософія дому', 'en' => 'Our philosophy'],
                    'secondary_cta_url' => '#manifesto',
                    'image_path' => 'pages/blocks/levant-luxury-bottle.jpg',
                    'meta' => [
                        ['num' => '22', 'meta_label' => ['uk' => 'Композиції', 'en' => 'Compositions']],
                        ['num' => '2', 'meta_label' => ['uk' => 'Серії', 'en' => 'Series']],
                        ['num' => '3', 'meta_label' => ['uk' => 'Країни', 'en' => 'Countries']],
                    ],
                ],
            ],

            // 2. Text (manifesto)
            [
                'type' => 'text',
                'data' => [
                    'is_visible' => true,
                    'anchor' => 'manifesto',
                    'eyebrow' => ['uk' => 'Філософія', 'en' => 'Philosophy'],
                    'title' => [
                        'uk' => "Якщо вам потрібне ім'я і красива коробка — вам у дьюті-фрі.",
                        'en' => 'If you need a name and a beautiful box — go to duty-free.',
                    ],
                    'body' => [
                        'uk' => "Ми ж — для тих, хто хоче аромат, а не бирку. 20 років у парфумерії дають нам знати, де купувати найкращі інгредієнти, — і ми це робимо.\n\nРозроблено в Іспанії. Розлито в Туреччині. Зібрано тут — в Україні. Без переплати за логотип, без подвоєної ціни за порожній флакон.",
                        'en' => "We are for those who want the scent, not the tag. Twenty years in perfumery taught us where to source the best ingredients — and we do.\n\nComposed in Spain. Bottled in Turkey. Assembled here, in Ukraine. No premium for a logo, no doubled price for an empty bottle.",
                    ],
                    'signature' => ['uk' => '— Команда Levant', 'en' => '— The Levant team'],
                ],
            ],

            // 3. Products (bestsellers)
            [
                'type' => 'products',
                'data' => [
                    'is_visible' => ! empty($bestsellerIds),
                    'eyebrow' => ['uk' => 'Бестселери', 'en' => 'Bestsellers'],
                    'title' => ['uk' => 'Найулюбленіші у 2026', 'en' => 'Most loved in 2026'],
                    'cta_label' => ['uk' => 'Усі бестселери', 'en' => 'All bestsellers'],
                    'cta_url' => '/products?sort=pop',
                    'items' => array_map(fn ($id) => ['product_id' => $id], $bestsellerIds),
                ],
            ],

            // 4. Brand story (three points)
            [
                'type' => 'brand_story',
                'data' => [
                    'is_visible' => true,
                    'eyebrow' => ['uk' => 'Три точки. Один путь.', 'en' => 'Three points. One signature.'],
                    'title' => [
                        'uk' => 'Levant — перетин трьох світів',
                        'en' => 'Levant — a crossing of three worlds',
                    ],
                    'body' => [
                        'uk' => 'Levant — давня назва регіону, де зустрічаються Схід і Захід, де торгівля, культура та аромати знаходили одне одного тисячоліттями.',
                        'en' => 'Levant is the ancient name of a region where East and West meet, where trade, culture and scent have found each other for millennia.',
                    ],
                    'pillars' => [
                        [
                            'pillar_label' => ['uk' => 'Іспанія', 'en' => 'Spain'],
                            'pillar_caption' => ['uk' => 'Народження ідеї', 'en' => 'Where the idea is born'],
                        ],
                        [
                            'pillar_label' => ['uk' => 'Туреччина', 'en' => 'Turkey'],
                            'pillar_caption' => ['uk' => 'Розливається тут', 'en' => 'Where it is bottled'],
                        ],
                        [
                            'pillar_label' => ['uk' => 'Україна', 'en' => 'Ukraine'],
                            'pillar_caption' => ['uk' => 'Ринок і душа', 'en' => 'The market and the soul'],
                        ],
                    ],
                ],
            ],

            // 5. Series duo (collections)
            [
                'type' => 'series_duo',
                'data' => [
                    'is_visible' => true,
                    'eyebrow' => ['uk' => 'Колекції', 'en' => 'Collections'],
                    'title' => ['uk' => 'Дві серії, одна філософія', 'en' => 'Two series, one philosophy'],
                    'items' => [
                        [
                            'series_id' => $luxury?->id,
                            'image_path' => 'pages/blocks/levant-flacon-3.jpg',
                            'kicker' => ['uk' => '17 ароматів · жіноча та унісекс', 'en' => '17 fragrances · women & unisex'],
                            'title' => ['uk' => 'Luxury Series', 'en' => 'Luxury Series'],
                            'description' => [
                                'uk' => 'Від легкого флорального шепоту до насиченої нішевої глибини. Жіноча й унісекс — для тієї, яка знає, чого хоче.',
                                'en' => 'From a light floral whisper to a deep niche core. Women and unisex — for those who know what they want.',
                            ],
                            'cta_label' => ['uk' => 'Перейти до серії', 'en' => 'Open the series'],
                        ],
                        [
                            'series_id' => $onyx?->id,
                            'image_path' => 'pages/blocks/levant-flacon-4.jpg',
                            'kicker' => ['uk' => '5 ароматів · чоловіча колекція', 'en' => "5 fragrances · men's collection"],
                            'title' => ['uk' => 'Onyx Series', 'en' => 'Onyx Series'],
                            'description' => [
                                'uk' => 'Для чоловіків, які не шукають уваги — і саме тому її отримують. Мінеральна глибина оніксу, вечірня стійкість, підпис без слів.',
                                'en' => 'For men who do not seek attention — and receive it precisely for that. The mineral depth of onyx, evening tenacity, a signature without words.',
                            ],
                            'cta_label' => ['uk' => 'Перейти до серії', 'en' => 'Open the series'],
                        ],
                    ],
                ],
            ],

            // 6. Pillars — Guide (tinted, 3 steps)
            [
                'type' => 'pillars',
                'data' => [
                    'is_visible' => true,
                    'surface' => 'tinted',
                    'eyebrow' => ['uk' => 'Гід', 'en' => 'Guide'],
                    'title' => ['uk' => 'Знайдіть аромат за три кроки', 'en' => 'Find a scent in three steps'],
                    'body' => ['uk' => '', 'en' => ''],
                    'items' => [
                        [
                            'eyebrow' => ['uk' => '01 · Сімейство', 'en' => '01 · Family'],
                            'title' => ['uk' => 'Сімейство', 'en' => 'Family'],
                            'body' => [
                                'uk' => 'Квіткові, східні, деревні, шкіряні, шипрові, зелені, гурманні. З чим ви проводите день?',
                                'en' => 'Floral, oriental, woody, leather, chypre, green, gourmand. What is the air like in your day?',
                            ],
                        ],
                        [
                            'eyebrow' => ['uk' => '02 · Інтенсивність', 'en' => '02 · Intensity'],
                            'title' => ['uk' => 'Інтенсивність', 'en' => 'Intensity'],
                            'body' => [
                                'uk' => 'Шлейф від інтимного до галерейного. Оберіть, наскільки вас має бути чути.',
                                'en' => 'Trail from intimate to gallery. Choose how loudly you want to be heard.',
                            ],
                        ],
                        [
                            'eyebrow' => ['uk' => '03 · Випадок', 'en' => '03 · Occasion'],
                            'title' => ['uk' => 'Випадок', 'en' => 'Occasion'],
                            'body' => [
                                'uk' => 'Робочий ранок, вечірня сукня, щоденне. Парфум знає різницю.',
                                'en' => 'Working morning, evening dress, everyday. Perfume knows the difference.',
                            ],
                        ],
                    ],
                ],
            ],

            // 7. Products (new arrivals)
            [
                'type' => 'products',
                'data' => [
                    'is_visible' => ! empty($newIds),
                    'eyebrow' => ['uk' => 'Новинки', 'en' => 'New arrivals'],
                    'title' => ['uk' => 'Свіже з лабораторії', 'en' => 'Just out of the lab'],
                    'cta_label' => ['uk' => 'Усі новинки', 'en' => 'All new'],
                    'cta_url' => '/products?sort=new',
                    'items' => array_map(fn ($id) => ['product_id' => $id], $newIds),
                ],
            ],

            // 8. Pillars — Advantages (default, 3 reasons)
            [
                'type' => 'pillars',
                'data' => [
                    'is_visible' => true,
                    'surface' => 'default',
                    'eyebrow' => ['uk' => 'Чому Levant', 'en' => 'Why Levant'],
                    'title' => ['uk' => 'Три причини обрати Levant', 'en' => 'Three reasons to choose Levant'],
                    'body' => ['uk' => '', 'en' => ''],
                    'items' => [
                        [
                            'eyebrow' => ['uk' => '', 'en' => ''],
                            'title' => ['uk' => 'Розроблено в Іспанії', 'en' => 'Composed in Spain'],
                            'body' => [
                                'uk' => '20 років досвіду парфумерної школи. Доступ до найкращих інгредієнтів — і ми це знаємо.',
                                'en' => 'Twenty years of perfumery school. Access to the best ingredients — and we know how to use them.',
                            ],
                        ],
                        [
                            'eyebrow' => ['uk' => '', 'en' => ''],
                            'title' => ['uk' => 'Доставка Новою Поштою', 'en' => 'Nova Poshta delivery'],
                            'body' => [
                                'uk' => 'Безкоштовно від 1 500 грн. Відправлення у день замовлення, якщо до 14:00.',
                                'en' => 'Free over 1 500 UAH. Same-day shipping if ordered before 14:00.',
                            ],
                        ],
                        [
                            'eyebrow' => ['uk' => '', 'en' => ''],
                            'title' => ['uk' => 'Повернення за 14 днів', 'en' => '14-day returns'],
                            'body' => [
                                'uk' => 'Якщо аромат не той — повернемо кошти без зайвих питань.',
                                'en' => 'If the scent is not yours — we refund without questions.',
                            ],
                        ],
                    ],
                ],
            ],

            // 9. Testimonials (slider)
            [
                'type' => 'testimonials',
                'data' => [
                    'is_visible' => true,
                    'eyebrow' => ['uk' => 'Відгуки', 'en' => 'Reviews'],
                    'title' => ['uk' => 'Що пишуть про Levant', 'en' => 'What is said about Levant'],
                    'cta_label' => ['uk' => 'Усі відгуки', 'en' => 'All reviews'],
                    'cta_url' => '#',
                    'items' => [
                        [
                            'quote' => [
                                'uk' => 'Onyx № 03 — це той випадок, коли тобі не треба представлятись. Шлейф розповідає за тебе.',
                                'en' => 'Onyx № 03 is the case when you do not need to introduce yourself. The trail does it for you.',
                            ],
                            'author' => 'Софія К.',
                            'city' => ['uk' => 'Київ', 'en' => 'Kyiv'],
                            'rating' => 5,
                        ],
                        [
                            'quote' => [
                                'uk' => 'Замовив Luxury № 02 у подарунок. Дружина впізнала аромат, але здивувалась ціні.',
                                'en' => 'I ordered Luxury № 02 as a gift. My wife recognised the scent — and was surprised by the price.',
                            ],
                            'author' => 'Тарас М.',
                            'city' => ['uk' => 'Львів', 'en' => 'Lviv'],
                            'rating' => 5,
                        ],
                        [
                            'quote' => [
                                'uk' => 'Перейшла на Levant з нішевих французьких марок і не шкодую. Якість на тому ж рівні, ціна — нижча.',
                                'en' => 'I moved from French niche houses to Levant — no regrets. Same quality, a lower price.',
                            ],
                            'author' => 'Анна П.',
                            'city' => ['uk' => 'Одеса', 'en' => 'Odesa'],
                            'rating' => 5,
                        ],
                        [
                            'quote' => [
                                'uk' => 'Onyx № 05 — найкращий зимовий аромат у моїй колекції. Тютюн і амбра — це геніально.',
                                'en' => 'Onyx № 05 — the best winter scent in my collection. Tobacco and amber — genius.',
                            ],
                            'author' => 'Іван Р.',
                            'city' => ['uk' => 'Харків', 'en' => 'Kharkiv'],
                            'rating' => 5,
                        ],
                    ],
                ],
            ],

            // 10. Articles (blog preview)
            [
                'type' => 'articles',
                'data' => [
                    'is_visible' => count($articleIds) >= 3,
                    'eyebrow' => ['uk' => 'Журнал', 'en' => 'Journal'],
                    'title' => [
                        'uk' => 'Свіже з нашого редакторського столу',
                        'en' => 'Fresh from our editorial desk',
                    ],
                    'cta_label' => ['uk' => 'Усі статті', 'en' => 'All articles'],
                    'cta_url' => '#',
                    'items' => array_map(fn ($id) => ['article_id' => $id], array_slice($articleIds, 0, 3)),
                ],
            ],
        ];
    }
}
```

- [ ] **Step 2: Прогон `migrate:fresh --seed` для проверки**

Run: `php artisan migrate:fresh --seed`
Expected: успешно. БД содержит главную, в `storage/app/public/pages/blocks/` лежат 3 jpg.

Verify:
```bash
ls -la storage/app/public/pages/blocks/
php artisan tinker --execute='echo \App\Models\Content\Page::homepage()->first()->blocks ? "10 blocks: ".count(\App\Models\Content\Page::homepage()->first()->blocks) : "no homepage";'
```
Expected: `10 blocks: 10` и 3 файла в storage.

- [ ] **Step 3: Коммит**

```bash
git add database/seeders/Content/PageSeeder.php
git commit -m "feat(homepage): seed 10-section landing page with copy from data.js"
```

---

## Task 34: Тест — `HomePageRenderTest`

**Files:**
- Create: `tests/Feature/Content/HomePageRenderTest.php`

Тестируем рендер всех 8 типов блоков на главной, fallback на uk, surface переключатель, скрытие через is_visible.

- [ ] **Step 1: Создать тест-файл**

```php
<?php

use App\Enums\PageTemplate;
use App\Models\Catalogue\Product;
use App\Models\Catalogue\Series;
use App\Models\Content\Article;
use App\Models\Content\Page;

function makeHomepage(array $blocks): Page
{
    return Page::query()->updateOrCreate(
        ['is_homepage' => true],
        [
            'slug' => ['uk' => 'home-uk', 'en' => 'home-en'],
            'title' => ['uk' => 'Головна', 'en' => 'Home'],
            'intro' => ['uk' => '', 'en' => ''],
            'content' => null,
            'is_published' => true,
            'template' => PageTemplate::Landing,
            'blocks' => $blocks,
        ],
    );
}

it('renders the hero block with editorial split markup', function () {
    makeHomepage([
        [
            'type' => 'hero',
            'data' => [
                'is_visible' => true,
                'eyebrow' => ['uk' => 'Колекція 2026', 'en' => 'Collection 2026'],
                'title_top' => ['uk' => 'Нішевий аромат.', 'en' => 'Niche fragrance.'],
                'title_bottom' => ['uk' => 'Чесна ціна.', 'en' => 'Honest price.'],
                'lead' => ['uk' => 'Lead UK', 'en' => 'Lead EN'],
                'meta' => [
                    ['num' => '22', 'meta_label' => ['uk' => 'Композиції', 'en' => 'Compositions']],
                    ['num' => '2', 'meta_label' => ['uk' => 'Серії', 'en' => 'Series']],
                    ['num' => '3', 'meta_label' => ['uk' => 'Країни', 'en' => 'Countries']],
                ],
            ],
        ],
    ]);

    $this->get('/uk')
        ->assertOk()
        ->assertSee('class="hero', false)
        ->assertSee('Колекція 2026')
        ->assertSee('Нішевий аромат.')
        ->assertSee('Чесна ціна.')
        ->assertSee('class="num">22<', false);
});

it('renders the manifesto (text) block', function () {
    makeHomepage([
        [
            'type' => 'text',
            'data' => [
                'is_visible' => true,
                'eyebrow' => ['uk' => 'Філософія', 'en' => 'Philosophy'],
                'title' => ['uk' => 'Заголовок маніфесту', 'en' => 'Manifesto title'],
                'body' => ['uk' => "Перший абзац.\n\nДругий абзац.", 'en' => "First.\n\nSecond."],
                'signature' => ['uk' => '— Команда', 'en' => '— Team'],
            ],
        ],
    ]);

    $response = $this->get('/uk')->assertOk();
    $response->assertSee('class="manifesto', false);
    $response->assertSee('Філософія');
    $response->assertSee('Заголовок маніфесту');
    $response->assertSee('Перший абзац.');
    $response->assertSee('Другий абзац.');
    $response->assertSee('— Команда');
});

it('renders the brand_story block with three pillars', function () {
    makeHomepage([
        [
            'type' => 'brand_story',
            'data' => [
                'is_visible' => true,
                'eyebrow' => ['uk' => 'Три точки', 'en' => 'Three points'],
                'title' => ['uk' => 'Перетин трьох світів', 'en' => 'Three worlds'],
                'body' => ['uk' => 'Опис', 'en' => 'Body'],
                'pillars' => [
                    ['pillar_label' => ['uk' => 'Іспанія', 'en' => 'Spain'], 'pillar_caption' => ['uk' => 'Ідея', 'en' => 'Idea']],
                    ['pillar_label' => ['uk' => 'Туреччина', 'en' => 'Turkey'], 'pillar_caption' => ['uk' => 'Розлив', 'en' => 'Bottling']],
                    ['pillar_label' => ['uk' => 'Україна', 'en' => 'Ukraine'], 'pillar_caption' => ['uk' => 'Ринок', 'en' => 'Market']],
                ],
            ],
        ],
    ]);

    $response = $this->get('/uk')->assertOk();
    $response->assertSee('class="threepoints', false);
    $response->assertSee('Іспанія');
    $response->assertSee('Туреччина');
    $response->assertSee('Україна');
    // 2 connectors between 3 points
    expect(substr_count($response->getContent(), 'class="conn"'))->toBe(2);
});

it('renders the series_duo block with safe series lookup', function () {
    Series::query()->updateOrCreate(['slug' => 'luxury'], ['name' => ['uk' => 'Luxury', 'en' => 'Luxury'], 'theme_class' => 'theme-cream']);

    makeHomepage([
        [
            'type' => 'series_duo',
            'data' => [
                'is_visible' => true,
                'eyebrow' => ['uk' => 'Колекції', 'en' => 'Collections'],
                'title' => ['uk' => 'Дві серії', 'en' => 'Two series'],
                'items' => [
                    [
                        'series_id' => Series::where('slug', 'luxury')->value('id'),
                        'kicker' => ['uk' => '17 ароматів', 'en' => '17 fragrances'],
                        'title' => ['uk' => 'Luxury Series', 'en' => 'Luxury Series'],
                        'description' => ['uk' => 'Опис', 'en' => 'Desc'],
                        'cta_label' => ['uk' => 'Перейти', 'en' => 'Open'],
                    ],
                    [
                        // stale series_id — must NOT throw
                        'series_id' => 999999,
                        'kicker' => ['uk' => 'X', 'en' => 'X'],
                        'title' => ['uk' => 'Onyx', 'en' => 'Onyx'],
                        'description' => ['uk' => 'D', 'en' => 'D'],
                        'cta_label' => ['uk' => 'Перейти', 'en' => 'Open'],
                    ],
                ],
            ],
        ],
    ]);

    $response = $this->get('/uk')->assertOk();
    $response->assertSee('class="collections', false);
    $response->assertSee('Luxury Series');
    $response->assertSee('Onyx');
});

it('renders the pillars block tinted when surface is tinted', function () {
    makeHomepage([
        [
            'type' => 'pillars',
            'data' => [
                'is_visible' => true,
                'surface' => 'tinted',
                'eyebrow' => ['uk' => 'Гід', 'en' => 'Guide'],
                'title' => ['uk' => 'Три кроки', 'en' => 'Three steps'],
                'items' => [
                    ['eyebrow' => ['uk' => '01', 'en' => '01'], 'title' => ['uk' => 'A', 'en' => 'A'], 'body' => ['uk' => 'a', 'en' => 'a']],
                    ['eyebrow' => ['uk' => '02', 'en' => '02'], 'title' => ['uk' => 'B', 'en' => 'B'], 'body' => ['uk' => 'b', 'en' => 'b']],
                    ['eyebrow' => ['uk' => '03', 'en' => '03'], 'title' => ['uk' => 'C', 'en' => 'C'], 'body' => ['uk' => 'c', 'en' => 'c']],
                ],
            ],
        ],
    ]);

    $this->get('/uk')
        ->assertOk()
        ->assertSee('class="pillars reveal is-tinted"', false)
        ->assertSee('data-count="3"', false);
});

it('renders the pillars block as advantages with data-count 4 and auto-numbers blank eyebrows', function () {
    makeHomepage([
        [
            'type' => 'pillars',
            'data' => [
                'is_visible' => true,
                'surface' => 'default',
                'eyebrow' => ['uk' => 'Чому ми', 'en' => 'Why us'],
                'title' => ['uk' => 'Чотири причини', 'en' => 'Four reasons'],
                'items' => [
                    ['title' => ['uk' => 'A', 'en' => 'A'], 'body' => ['uk' => 'a', 'en' => 'a']],
                    ['title' => ['uk' => 'B', 'en' => 'B'], 'body' => ['uk' => 'b', 'en' => 'b']],
                    ['title' => ['uk' => 'C', 'en' => 'C'], 'body' => ['uk' => 'c', 'en' => 'c']],
                    ['title' => ['uk' => 'D', 'en' => 'D'], 'body' => ['uk' => 'd', 'en' => 'd']],
                ],
            ],
        ],
    ]);

    $response = $this->get('/uk')->assertOk();
    $response->assertSee('data-count="4"', false);
    // Auto-numbered eyebrows: 01, 02, 03, 04
    $response->assertSee('>01<', false);
    $response->assertSee('>04<', false);
});

it('renders the testimonials block as horizontal slider', function () {
    makeHomepage([
        [
            'type' => 'testimonials',
            'data' => [
                'is_visible' => true,
                'eyebrow' => ['uk' => 'Відгуки', 'en' => 'Reviews'],
                'title' => ['uk' => 'Що пишуть', 'en' => 'What they say'],
                'items' => [
                    [
                        'quote' => ['uk' => 'Чудовий аромат', 'en' => 'Wonderful scent'],
                        'author' => 'Софія К.',
                        'city' => ['uk' => 'Київ', 'en' => 'Kyiv'],
                        'rating' => 5,
                    ],
                    [
                        'quote' => ['uk' => 'Подобається', 'en' => 'I like it'],
                        'author' => 'Тарас М.',
                        'city' => ['uk' => 'Львів', 'en' => 'Lviv'],
                        'rating' => 4,
                    ],
                ],
            ],
        ],
    ]);

    $response = $this->get('/uk')->assertOk();
    $response->assertSee('class="testimonials', false);
    $response->assertSee('class="track"', false);
    $response->assertSee('Чудовий аромат');
    $response->assertSee('Софія К.');
    $response->assertSee('· Київ');
});

it('renders the articles block as 3-card grid and limits to 3 even with more items', function () {
    $articles = Article::factory()->count(4)->create();
    makeHomepage([
        [
            'type' => 'articles',
            'data' => [
                'is_visible' => true,
                'eyebrow' => ['uk' => 'Журнал', 'en' => 'Journal'],
                'title' => ['uk' => 'Свіже', 'en' => 'Fresh'],
                'items' => $articles->map(fn ($a) => ['article_id' => $a->id])->all(),
            ],
        ],
    ]);

    $response = $this->get('/uk')->assertOk();
    $response->assertSee('class="blog', false);
    // 3 cards rendered even though 4 article_ids were provided
    expect(substr_count($response->getContent(), 'class="article-card"'))->toBe(3);
});

it('renders the products block as slider when items exist', function () {
    $products = Product::factory()->count(3)->create();
    makeHomepage([
        [
            'type' => 'products',
            'data' => [
                'is_visible' => true,
                'eyebrow' => ['uk' => 'Бестселери', 'en' => 'Bestsellers'],
                'title' => ['uk' => 'Найкращі', 'en' => 'Top'],
                'items' => $products->map(fn ($p) => ['product_id' => $p->id])->all(),
            ],
        ],
    ]);

    $response = $this->get('/uk')->assertOk();
    $response->assertSee('class="product-slider', false);
    $response->assertSee('Бестселери');
});

it('hides blocks with is_visible false', function () {
    makeHomepage([
        [
            'type' => 'hero',
            'data' => [
                'is_visible' => false,
                'eyebrow' => ['uk' => 'INVISIBLE', 'en' => 'INVISIBLE'],
                'title_top' => ['uk' => 'Hidden', 'en' => 'Hidden'],
            ],
        ],
    ]);

    $this->get('/uk')
        ->assertOk()
        ->assertDontSee('INVISIBLE')
        ->assertDontSee('Hidden');
});

it('falls back to uk when active locale string is empty (not null)', function () {
    makeHomepage([
        [
            'type' => 'hero',
            'data' => [
                'is_visible' => true,
                'eyebrow' => ['uk' => 'Колекція UK', 'en' => ''], // empty EN string, not null
                'title_top' => ['uk' => 'Тайтл UK', 'en' => 'Title EN'],
            ],
        ],
    ]);

    $this->get('/en')
        ->assertOk()
        ->assertSee('Колекція UK')  // EN falls back to UK because '' is blank
        ->assertSee('Title EN');     // EN renders normally because filled
});
```

- [ ] **Step 2: Прогнать тест**

Run: `php artisan test --filter='HomePageRender' -v`
Expected: всё PASS. Если что-то падает — разбираемся: чаще всего это селектор, который не совпал с фактической разметкой.

- [ ] **Step 3: Коммит**

```bash
git add tests/Feature/Content/HomePageRenderTest.php
git commit -m "test(homepage): render tests for all 8 block types + fallbacks + surface variants"
```

---

## Task 35: Тест — `PageBuilderHomeTest` (Filament)

**Files:**
- Create: `tests/Feature/Content/Filament/PageBuilderHomeTest.php`

Тестируем что Filament Builder содержит все 8 типов блоков, и что репитеры min/max работают (3 case'а: pillars, series_duo, brand_story).

- [ ] **Step 1: Создать тест-файл**

```php
<?php

use App\Filament\Resources\Pages\Pages\EditPage;
use App\Filament\Resources\Pages\Schemas\Blocks\ArticlesBlock;
use App\Filament\Resources\Pages\Schemas\Blocks\BrandStoryBlock;
use App\Filament\Resources\Pages\Schemas\Blocks\HeroBlock;
use App\Filament\Resources\Pages\Schemas\Blocks\PillarsBlock;
use App\Filament\Resources\Pages\Schemas\Blocks\ProductsBlock;
use App\Filament\Resources\Pages\Schemas\Blocks\SeriesDuoBlock;
use App\Filament\Resources\Pages\Schemas\Blocks\TestimonialsBlock;
use App\Filament\Resources\Pages\Schemas\Blocks\TextBlock;
use App\Models\User;
use Filament\Forms\Components\Builder;

beforeEach(function () {
    $admin = User::factory()->create();
    $this->actingAs($admin);
});

it('exposes 8 block types on the Builder field', function () {
    $blockClasses = [
        HeroBlock::class,
        TextBlock::class,
        ProductsBlock::class,
        BrandStoryBlock::class,
        SeriesDuoBlock::class,
        PillarsBlock::class,
        TestimonialsBlock::class,
        ArticlesBlock::class,
    ];

    $blockNames = collect($blockClasses)->map(fn ($cls) => $cls::make()->getName())->all();

    expect($blockNames)->toEqualCanonicalizing([
        'hero', 'text', 'products', 'brand_story', 'series_duo', 'pillars', 'testimonials', 'articles',
    ]);
});

it('enforces brand_story pillars exactly 3 via repeater minItems/maxItems', function () {
    $block = BrandStoryBlock::make();
    $schema = $block->getChildComponents();
    $pillarsRepeater = collect($schema)->first(fn ($c) => method_exists($c, 'getName') && $c->getName() === 'pillars');

    expect($pillarsRepeater)->not->toBeNull()
        ->and($pillarsRepeater->getMinItems())->toBe(3)
        ->and($pillarsRepeater->getMaxItems())->toBe(3);
});

it('enforces series_duo items exactly 2', function () {
    $block = SeriesDuoBlock::make();
    $schema = $block->getChildComponents();
    $itemsRepeater = collect($schema)->first(fn ($c) => method_exists($c, 'getName') && $c->getName() === 'items');

    expect($itemsRepeater)->not->toBeNull()
        ->and($itemsRepeater->getMinItems())->toBe(2)
        ->and($itemsRepeater->getMaxItems())->toBe(2);
});

it('enforces pillars items min 3 and max 4', function () {
    $block = PillarsBlock::make();
    $schema = $block->getChildComponents();
    $itemsRepeater = collect($schema)->first(fn ($c) => method_exists($c, 'getName') && $c->getName() === 'items');

    expect($itemsRepeater)->not->toBeNull()
        ->and($itemsRepeater->getMinItems())->toBe(3)
        ->and($itemsRepeater->getMaxItems())->toBe(4);
});

it('enforces hero meta exactly 3', function () {
    $block = HeroBlock::make();
    $schema = $block->getChildComponents();
    $metaRepeater = collect($schema)->first(fn ($c) => method_exists($c, 'getName') && $c->getName() === 'meta');

    expect($metaRepeater)->not->toBeNull()
        ->and($metaRepeater->getMinItems())->toBe(3)
        ->and($metaRepeater->getMaxItems())->toBe(3);
});

it('enforces articles items exactly 3', function () {
    $block = ArticlesBlock::make();
    $schema = $block->getChildComponents();
    $itemsRepeater = collect($schema)->first(fn ($c) => method_exists($c, 'getName') && $c->getName() === 'items');

    expect($itemsRepeater)->not->toBeNull()
        ->and($itemsRepeater->getMinItems())->toBe(3)
        ->and($itemsRepeater->getMaxItems())->toBe(3);
});
```

**Note:** Filament API для интроспекции репитеров (`getMinItems()`, `getMaxItems()`, `getChildComponents()`) — публичный, доступен в Filament v5. Если в текущей минорной версии тестируемого проекта какие-то методы переименованы (например, `getChildSchema()` вместо `getChildComponents()`), исправить локально. Сначала прогнать тест и посмотреть фактическую ошибку.

- [ ] **Step 2: Прогнать тест**

Run: `php artisan test --filter='PageBuilderHome' -v`
Expected: всё PASS. Если ошибка по `getChildComponents` → заменить на корректный метод Filament (см. issue выше).

- [ ] **Step 3: Коммит**

```bash
git add tests/Feature/Content/Filament/PageBuilderHomeTest.php
git commit -m "test(homepage): Filament Builder block contract tests"
```

---

## Task 36: Финальная верификация — полный прогон

**Files:** none (только проверки)

- [ ] **Step 1: Полный тест-прогон**

Run: `composer test`
Expected: всё зелёное, включая `HomePageRenderTest`, `PageBuilderHomeTest` и существующие тесты Page/Article/Filament/Catalogue.

- [ ] **Step 2: migrate:fresh --seed**

Run: `php artisan migrate:fresh --seed`
Expected: успешно. Проверить:

```bash
ls -la storage/app/public/pages/blocks/
php artisan tinker --execute='$p = \App\Models\Content\Page::homepage()->first(); echo "blocks: ".count($p->blocks).PHP_EOL; echo "types: ".implode(",", array_column($p->blocks, "type")).PHP_EOL;'
```

Expected: 3 jpg в storage; `blocks: 10`; `types: hero,text,products,brand_story,series_duo,pillars,products,pillars,testimonials,articles`.

- [ ] **Step 3: Запустить dev-сервер и пройти главную в браузере**

В одном терминале:
```bash
composer dev
```

В браузере открыть:
- `http://127.0.0.1:8000/uk` — главная должна иметь все 10 секций в порядке hero → manifesto → products(best) → threepoints → collections → guide(tinted pillars) → products(new) → advantages(default pillars, 3-col) → testimonials → blog(articles).
- `http://127.0.0.1:8000/en` — то же содержание на английском.
- Hero: 2 CTA (gold + ghost), floating tag вверху справа, 3-up meta снизу.
- Manifesto: tinted bg-2, italic h2 с `“`, signature золотая курсивная.
- Threepoints: 3 gem'а с золотыми коннекторами.
- Collections: 2 карточки 4:5 с overlay — клик ведёт на `/uk/products?series=luxury` и `?series=onyx`.
- Guide (первый pillars): tinted bg-2, 3 cards с deco-уголками.
- Advantages (второй pillars): без фона, 3 ячейки разделены тонкими дивайдерами.
- Testimonials: горизонтальный slider с gold quote-mark и звёздами.
- Blog: 3 article-card'а в ряд.

- [ ] **Step 4: Админка**

Открыть `http://127.0.0.1:8000/admin/login`, войти под `admin@levantparfums.test / password`.
Перейти `/admin/pages`, открыть «Головна» → Edit.
- Переключатель `template = landing` показывает Builder с 10 уже сохранёнными блоками.
- Кнопка «Add block» показывает 8 типов с правильными иконками и метками.
- Открыть hero → переключить LocaleSwitcher в EN → поля переключаются на английские значения.
- Открыть pillars (Guide) → поле `surface = tinted`. Сменить на `default`, сохранить → на `/uk` фон секции исчезает.
- Открыть hero → попытаться удалить meta-item → кнопка delete недоступна на 3-м (minItems=3).

- [ ] **Step 5: Финальный коммит-меточка (опционально)**

Если все шаги зелёные, добавить тэг или просто финальный «closing» commit:

```bash
git commit --allow-empty -m "homepage: implementation complete, verified end-to-end"
```

---

## Self-Review (после написания плана)

**1. Spec coverage:**

- ✅ § «Архитектура» — все enum/Block-классы/партиалы/компоненты/CSS-файлы покрыты Task 1, 3-6, 7-10, 12-14, 15-22, 23-29, 30.
- ✅ § «Контракты данных блоков» — каждый блок имеет свой Filament Task (3-10) с точными полями.
- ✅ § «Filament — Block-классы и PageForm» — Task 11 регистрирует все 8 блоков; иконки из спеки (heroicon-o-map/squares-2x2/list-bullet/chat-bubble) — присутствуют.
- ✅ § «Frontend — партиалы, компоненты, CSS» — Tasks 12-29.
- ✅ § «Theming и фоновый ритм» — CSS Task 24 (manifesto bg-2), Task 27 (pillars.is-tinted), Task 28 (testimonials bg-2).
- ✅ § «PageSeeder» — Task 33 копирует assets и пишет 10 секций. Task 31 фиксит порядок сидеров. Task 32 копирует assets в `database/seeders/images`.
- ✅ § «Переводы» — Task 2.
- ✅ § «Тесты» — Tasks 34-35 покрывают все названные тесты (8 render-тестов + 6 контракт-тестов).
- ✅ § «Порядок реализации» — следует за Tasks 1-36 в логическом порядке.
- ✅ Post-review fixes (`filled()` fallback, реордер сидеров, safe Series lookup, unicode read-time) — все интегрированы: Task 13 (read-time), Task 17/19/etc (`filled()` helper в каждом партиале), Task 19 (safe Series lookup), Task 31 (реордер).

**2. Placeholder scan:** Ни одного TBD/TODO/«добавить позже». Все code-блоки целиком. Все команды конкретные.

**3. Type consistency:** Имена полей сквозные:
- `title_top`/`title_bottom` упоминаются в спеке, в HeroBlock (Task 7), в hero партиале (Task 15), в seeder (Task 33), в тесте (Task 34). ✓
- `pillar_label`/`pillar_caption` — BrandStoryBlock (Task 3), brand_story партиал (Task 18), seeder (Task 33), тест (Task 34). ✓
- `meta_label` (translatable) внутри `meta` repeater — HeroBlock (Task 7), hero партиал (Task 15), seeder (Task 33), тест (Task 34). ✓
- `pillars` (имя repeater'а в brand_story) НЕ совпадает с `items` (имя repeater'а в pillars-блоке) — это намеренно: brand_story имеет `pillars`, а pillars-блок имеет `items`. Тест проверяет именно эти разные имена.
- Имена компонентов: `<x-site.pillar>` принимает `variant`, `eyebrow`, `label`, `caption`, `title`, `body` — используется и в brand_story (Task 18: label/caption + variant=brand_story), и в pillars (Task 20: eyebrow/title/body + variant=pillars). ✓
- `surface = default|tinted` — PillarsBlock (Task 5), pillars партиал (Task 20), pillars.css (Task 27), seeder (Task 33), тест (Task 34). ✓
- Класс `.is-tinted` появляется ровно в Task 20 (Blade) и Task 27 (CSS). ✓
- `data-count` атрибут появляется в Task 20 (Blade) и Task 27 (CSS-селектор). ✓

**4. Ambiguity:**
- В Task 12 решено, что `<x-site.pillar>` универсальный с `variant`. Это согласовано со спекой (там сказано «Альтернатива: два отдельных компонента. Решено объединить в один с variant»).
- В Task 35 явно отмечено, что Filament API мог поменять имена методов (`getChildComponents`) — добавлена инструкция «прогнать и посмотреть фактическую ошибку».
- В Task 13 явно отмечено, что маршрут `articles.show` может отсутствовать → fallback на `#`.

План закрыт.

---

## Verification Checklist (для финального ревью)

После Task 36 пройти:

- [ ] `composer test` — все зелёное.
- [ ] `php artisan migrate:fresh --seed` — БД поднимается, в storage 3 jpg, главная имеет 10 блоков.
- [ ] `npm run build` — собирается без ошибок.
- [ ] `/uk` и `/en` визуально соответствуют `pages-home.jsx` (с учётом testimonials как slider).
- [ ] Админка показывает 8 типов блоков; редактирование сохраняется.
- [ ] Surface tinted/default в Guide-блоке переключает фон.
